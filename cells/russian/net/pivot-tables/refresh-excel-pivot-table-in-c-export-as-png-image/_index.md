---
category: general
date: 2026-02-23
description: Обновить сводную таблицу Excel в C# и экспортировать её как PNG‑изображение.
  Узнайте, как загрузить книгу Excel в C#, обновить сводную таблицу и сохранить результат.
draft: false
keywords:
- refresh excel pivot table
- load excel workbook c#
- export pivot as image
- export excel pivot image
language: ru
og_description: Обновление сводной таблицы Excel в C# и экспорт её в PNG‑изображение.
  Пошаговое руководство с полным кодом и практическими советами.
og_title: Обновление сводной таблицы Excel в C# – экспорт в PNG‑изображение
tags:
- C#
- Excel
- Aspose.Cells
- Data Automation
title: Обновление сводной таблицы Excel в C# – экспорт в PNG‑изображение
url: /ru/net/pivot-tables/refresh-excel-pivot-table-in-c-export-as-png-image/
---

{{< blocks/products/pf/main-wrap-class >}}
{{< blocks/products/pf/main-container >}}
{{< blocks/products/pf/tutorial-page-section >}}

# Обновление сводной таблицы Excel в C# – Экспорт в PNG‑изображение

Когда‑нибудь вам нужно было **обновить сводную таблицу Excel** из приложения на C# и затем превратить её в изображение? Вы не одиноки в этой проблеме. В этом руководстве мы подробно покажем, как **refresh Excel pivot table**, **load Excel workbook C#**, и, наконец, **export pivot as image** — всё в чистом, готовом к запуску фрагменте кода.

В итоге вы получите PNG‑файл, который выглядит точно так же, как сводная таблица в Excel, готовый к встраиванию в отчёты, письма или панели мониторинга. Никакого ручного копирования‑вставки, никаких сложных COM‑взаимодействий, просто прямой .NET‑код.

## Предварительные требования

- .NET 6+ (или .NET Framework 4.7+)
- Aspose.Cells for .NET (бесплатная пробная версия или лицензированная) – её можно получить из NuGet с помощью `Install-Package Aspose.Cells`.
- Существующий файл `input.xlsx`, содержащий как минимум одну сводную таблицу.
- Папка, в которой у вас есть права записи для выходного изображения.

> **Совет:** Если вы используете Visual Studio, включите **nullable reference types** (`<Nullable>enable</Nullable>`), чтобы раннее обнаруживать ошибки, связанные с null.

---

## Шаг 1: Загрузка книги Excel в C#

Первое, что нам нужно, — объект `Workbook`, указывающий на наш исходный файл. Считайте это программным открытием файла Excel.

```csharp
using System;
using Aspose.Cells;

class PivotExportDemo
{
    static void Main()
    {
        // 👉 Step 1: Load the workbook and obtain the first worksheet
        string inputPath = @"YOUR_DIRECTORY\input.xlsx";
        Workbook wb = new Workbook(inputPath);
        Worksheet worksheet = wb.Worksheets[0];

        // The rest of the steps follow…
```

**Почему это важно:** Загрузка книги даёт доступ к листам, ячейкам и — самое главное — к созданным вами сводным таблицам. Если файл не найден, Aspose генерирует понятное исключение `FileNotFoundException`, которое можно перехватить для корректного отката.

---

## Шаг 2: Настройка параметров экспорта изображения (Экспорт сводной таблицы как изображения)

Aspose.Cells позволяет задать, как должна быть отрисована сводная таблица. Здесь мы выбираем PNG, поскольку он без потерь и широко поддерживается.

```csharp
        // 👉 Step 2: Configure image export options to use PNG format
        ImageOrPrintOptions imgOptions = new ImageOrPrintOptions
        {
            ImageFormat = ImageFormat.Png,
            // Optional: set resolution for sharper output
            HorizontalResolution = 300,
            VerticalResolution = 300
        };
```

**Почему PNG?** В отличие от JPEG, PNG сохраняет чёткие линии сетки и затенение текста, от которых зависят сводные таблицы. Если нужен более маленький файл, можно переключиться на `ImageFormat.Jpeg` и настроить качество, но при этом часть чёткости будет потеряна.

---

## Шаг 3: Обновление сводной таблицы

Прежде чем захватывать визуальное представление, необходимо убедиться, что сводная таблица отражает актуальные данные. Это и есть суть **refresh excel pivot table**.

```csharp
        // 👉 Step 3: Refresh the first pivot table so it reflects the latest data
        PivotTable pivot = worksheet.PivotTables[0];
        pivot.Refresh();
```

**Что происходит под капотом?** `Refresh()` пересчитывает сводную таблицу на основе диапазона источника. Если после сохранения книги вы добавили строки в исходные данные, этот вызов подтянет их. Пропуск этого шага приводит к устаревшему изображению, не соответствующему текущим данным.

---

## Шаг 4: Рендеринг сводной таблицы в PNG (Экспорт изображения сводной таблицы Excel)

Теперь, когда всё актуально, мы можем отрисовать сводную таблицу непосредственно в файл изображения.

```csharp
        // 👉 Step 4: Export the refreshed pivot table as a PNG image
        string outputPath = @"YOUR_DIRECTORY\pivot.png";
        pivot.RenderToImage(imgOptions, outputPath);

        Console.WriteLine($"Pivot table exported successfully to: {outputPath}");
    }
}
```

**Результат:** Откройте `pivot.png`, и вы увидите пиксель‑точную копию обновлённой сводной таблицы. Этот файл можно прикрепить к письму, встроить в веб‑страницу или передать в движок отчётности.

### Ожидаемый результат

```
Pivot table exported successfully to: YOUR_DIRECTORY\pivot.png
```

Если перейти в папку, PNG‑файл будет отображать те же строки, столбцы и фильтры, что и в Excel.

---

## Обработка распространённых граничных случаев

| Ситуация | Что делать |
|-----------|------------|
| **Несколько сводных таблиц** | Пройдитесь по `worksheet.PivotTables` и вызовите `Refresh()` / `RenderToImage()` для каждой. |
| **Динамические имена листов** | Используйте `wb.Worksheets[wb.Worksheets.IndexOf("SheetName")]` или ищите по `worksheet.Name`. |
| **Большие наборы данных** | Установите `imgOptions.OnePagePerSheet = false` и задайте `imgOptions.PageWidth`/`PageHeight` для управления разбиением на страницы. |
| **Отсутствует лицензия Aspose.Cells** | Бесплатная пробная версия добавляет водяной знак. Приобретите лицензию и вызовите `License license = new License(); license.SetLicense("Aspose.Total.NET.lic");` перед загрузкой книги. |
| **Проблемы с путями к файлам** | Используйте `Path.Combine(Environment.CurrentDirectory, "input.xlsx")`, чтобы избежать жёстко заданных разделителей. |

---

## Советы и лучшие практики

- **Правильное освобождение ресурсов** – Оберните `Workbook` в блок `using` или вызовите `wb.Dispose()` после завершения, чтобы освободить нативные ресурсы.
- **Кешировать отрисованные изображения** – Если вам нужен один и тот же образ сводной таблицы многократно, сохраняйте PNG на диск и переиспользуйте его вместо повторного рендеринга.
- **Безопасность потоков** – Каждый поток должен работать со своим экземпляром `Workbook`; объекты Aspose.Cells не являются потокобезопасными.
- **Производительность** – Рендеринг больших сводных таблиц может требовать много памяти. Установите `imgOptions.ImageFormat` в `Bmp` для более быстрой, но более крупной файловой записи, либо уменьшите DPI для ускорения рендеринга.

---

## Полный рабочий пример (готовый к копированию и вставке)

```csharp
using System;
using System.IO;
using Aspose.Cells;
using Aspose.Cells.Rendering;

class PivotExportDemo
{
    static void Main()
    {
        // 👉 Step 1: Load the workbook and obtain the first worksheet
        string inputPath = Path.Combine(Environment.CurrentDirectory, "input.xlsx");
        if (!File.Exists(inputPath))
        {
            Console.Error.WriteLine($"File not found: {inputPath}");
            return;
        }

        Workbook wb = new Workbook(inputPath);
        Worksheet worksheet = wb.Worksheets[0];

        // 👉 Step 2: Configure image export options to use PNG format
        ImageOrPrintOptions imgOptions = new ImageOrPrintOptions
        {
            ImageFormat = ImageFormat.Png,
            HorizontalResolution = 300,
            VerticalResolution = 300
        };

        // 👉 Step 3: Refresh the first pivot table so it reflects the latest data
        if (worksheet.PivotTables.Count == 0)
        {
            Console.Error.WriteLine("No pivot tables found on the first worksheet.");
            return;
        }

        PivotTable pivot = worksheet.PivotTables[0];
        pivot.Refresh();

        // 👉 Step 4: Export the refreshed pivot table as a PNG image
        string outputPath = Path.Combine(Environment.CurrentDirectory, "pivot.png");
        pivot.RenderToImage(imgOptions, outputPath);

        Console.WriteLine($"Pivot table exported successfully to: {outputPath}");

        // Clean up
        wb.Dispose();
    }
}
```

Запустите программу, откройте `pivot.png`, и вы увидите обновлённую сводную таблицу точно так же, как в Excel.

---

## Часто задаваемые вопросы

**В: Работает ли это с файлами .xlsx, созданными в LibreOffice?**  
**О:** Да. Aspose.Cells читает формат Open XML независимо от приложения‑источника, поэтому вы можете **load excel workbook c#** из LibreOffice, экспорта Google Sheets или любого другого источника.

**В: Можно ли экспортировать несколько листов одновременно?**  
**О:** Конечно. Пройдитесь по `wb.Worksheets` и примените ту же логику `RenderToImage` к каждому листу. Только не забудьте давать каждому файлу уникальное имя.

**В: Что если сводная таблица использует внешний источник данных?**  
**О:** Aspose.Cells может обновлять внешние соединения, если они встроены в файл, но вам потребуется программно предоставить строку подключения и учётные данные. См. документацию Aspose по `DataSourceOptions`.

---

## Заключение

Теперь у вас есть надёжное, сквозное решение для **refresh excel pivot table** из C# и **export excel pivot image** в PNG. Код демонстрирует, как **load excel workbook c#**, настроить параметры изображения, убедиться, что сводная таблица отражает актуальные данные, и в конце отрисовать её в файл.

Далее вы можете исследовать **export pivot as image** в других форматах (PDF, SVG) или автоматизировать процесс для нескольких книг в пакетной задаче. Хотите встроить PNG в отчёт Word? Тот же класс `ImageOrPrintOptions` работает с Aspose.Words.

Не стесняйтесь экспериментировать, ломать вещи и задавать вопросы в комментариях — удачной разработки! 

![Refresh Excel pivot table screenshot](image.png)

{{< /blocks/products/pf/tutorial-page-section >}}
{{< /blocks/products/pf/main-container >}}
{{< /blocks/products/pf/main-wrap-class >}}
{{< blocks/products/products-backtop-button >}}