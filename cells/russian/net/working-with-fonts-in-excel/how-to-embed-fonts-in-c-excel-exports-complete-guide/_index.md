---
category: general
date: 2026-02-15
description: Узнайте, как встраивать шрифты при экспорте Excel в SVG и XPS, правильно
  записывать символы Unicode и встраивать шрифты в SVG с помощью Aspose.Cells.
draft: false
keywords:
- how to embed fonts
- export excel to svg
- how to write unicode
- embed fonts in svg
- how to export xps
language: ru
og_description: Как встраивать шрифты при экспорте Excel в SVG и XPS, записывать символы
  Unicode и встраивать шрифты в SVG с помощью Aspose.Cells.
og_title: Как внедрить шрифты в экспорт Excel из C# – пошагово
tags:
- Aspose.Cells
- C#
- Excel Export
- Font Embedding
title: Как встраивать шрифты в экспорт Excel из C# — Полное руководство
url: /ru/net/working-with-fonts-in-excel/how-to-embed-fonts-in-c-excel-exports-complete-guide/
---

{{< blocks/products/pf/main-wrap-class >}}
{{< blocks/products/pf/main-container >}}
{{< blocks/products/pf/tutorial-page-section >}}

# Как встраивать шрифты в экспорт Excel из C# – Полное руководство

Когда‑то задавались вопросом **как встраивать шрифты** в экспорт Excel, чтобы результат выглядел одинаково на любой машине? Вы не одиноки. Если вы отправляете лист клиенту, у которого не установлены те же шрифты, документ может выглядеть искажённым, особенно если в нём есть специальные Unicode‑символы. В этом руководстве мы пошагово рассмотрим практическое решение, которое не только показывает **как встраивать шрифты**, но также охватывает **export excel to svg**, **how to write unicode** и **how to export xps** с помощью Aspose.Cells.  

К концу руководства у вас будет готовый фрагмент кода C#, который записывает Unicode‑символ с селектором варианта, встраивает необходимые шрифты и создаёт файлы XPS и SVG, корректно отображающиеся везде. Без внешних инструментов, без пост‑обработки — только чистый, автономный код.

## Требования

- .NET 6.0 или новее (API работает одинаково и в .NET Framework 4.8)
- Aspose.Cells for .NET (NuGet‑пакет `Aspose.Cells`)
- Папка на диске, куда можно сохранять сгенерированные файлы
- Базовое знакомство с синтаксисом C# (если вы полный новичок, код снабжён обширными комментариями)

Если всё уже готово, отлично — сразу переходим к реализации.

## Шаг 1: Настройка Workbook и Worksheet (How to Embed Fonts – The Starting Point)

Первое, что нам нужно, — свежий объект `Workbook`. Представьте workbook как контейнер для всех листов, стилей и ресурсов. Создать его trivially, но это фундамент для любой операции **embed fonts in svg**, потому что информация о шрифте хранится на уровне workbook.

```csharp
using Aspose.Cells;

namespace FontEmbeddingDemo
{
    class Program
    {
        static void Main(string[] args)
        {
            // Step 1: Create a new workbook and grab the first worksheet
            Workbook workbook = new Workbook();               // fresh workbook
            Worksheet ws = workbook.Worksheets[0];            // default sheet
```

> **Почему это важно:** При последующем экспорте в SVG или XPS Aspose.Cells смотрит на коллекцию стилей workbook, чтобы решить, какие шрифты встраивать. Начало с чистого workbook гарантирует отсутствие посторонних ссылок на шрифты в выводе.

## Шаг 2: Записать Unicode‑символ с селектором варианта (How to Write Unicode)

Unicode‑символы могут быть сложными, особенно когда нужен конкретный глиф‑вариант. Символ `𝟘` (MATHEMATICAL DOUBLE‑STRUCK ZERO) в сочетании с селектором варианта‑1 (`\uFE00`) заставляет рендерер выбрать «простой» вариант отображения. Это идеальная демонстрация **how to write unicode**, показывающая точную строку, которую нужно поместить в ячейку.

```csharp
            // Step 2: Write the character '𝟘' followed by Variation Selector-1 into cell A1
            // The literal "\uFE00" is the Variation Selector; it tells the font to use the base glyph.
            ws.Cells["A1"].PutValue("𝟘\uFE00");
```

> **Совет:** Если в выводе появляется квадрат с вопросительным знаком (�), проверьте, поддерживает ли целевой шрифт базовый символ *и* селектор варианта. Не все шрифты это делают.

## Шаг 3: Экспорт листа в XPS (How to Export XPS)

XPS — формат фиксированной разметки, похожий на PDF, но нативный для Windows. Экспорт в XPS с **embedding fonts** гарантирует, что документ будет выглядеть одинаково на любой Windows‑машине, даже если шрифт не установлен локально.

```csharp
            // Step 3: Export the worksheet to XPS – fonts are embedded automatically
            string xpsPath = @"C:\Exports\VarSel.xps";
            ws.Cells.ExportToXps(xpsPath);
```

> **Что вы увидите:** Откройте полученный `VarSel.xps` в Windows Reader; двойной ноль будет выглядеть точно так же, как в Excel, со сохранённым стилем.

## Шаг 4: Экспорт листа в SVG с встраиванием шрифтов (Embed Fonts in SVG)

SVG — векторный формат изображений, который браузеры рендерят «на лету». По умолчанию Aspose.Cells будет ссылаться на шрифт по имени, что может привести к отсутствию глифов, если у просмотрщика шрифт не установлен. Класс `SvgSaveOptions` позволяет **embed fonts in SVG**, превращая файл в автономный пакет.

```csharp
            // Step 4: Export to SVG with fonts embedded
            string svgPath = @"C:\Exports\VarSel.svg";
            SvgSaveOptions svgOptions = new SvgSaveOptions
            {
                EmbedFonts = true          // crucial flag – forces font embedding
            };
            ws.Cells.ExportToSvg(svgPath, svgOptions);
```

> **Результат:** Откройте `VarSel.svg` в любом современном браузере (Chrome, Edge, Firefox). Unicode‑символ отобразится корректно без внешних файлов шрифтов. Если посмотреть исходный SVG, вы увидите блок `<style>` с Base64‑закодированным определением шрифта.

## Полный рабочий пример (Все шаги вместе)

Ниже полностью готовая программа, которую можно скопировать в консольное приложение. Она включает все перечисленные шаги и выводит финальное сообщение в консоль, чтобы вы знали, когда процесс завершён.

```csharp
using Aspose.Cells;
using System;

namespace FontEmbeddingDemo
{
    class Program
    {
        static void Main(string[] args)
        {
            // Create workbook and worksheet
            Workbook workbook = new Workbook();
            Worksheet ws = workbook.Worksheets[0];

            // Write Unicode character with variation selector
            ws.Cells["A1"].PutValue("𝟘\uFE00");

            // Export to XPS (fonts embedded automatically)
            string xpsPath = @"C:\Exports\VarSel.xps";
            ws.Cells.ExportToXps(xpsPath);
            Console.WriteLine($"XPS exported to: {xpsPath}");

            // Export to SVG with embedded fonts
            string svgPath = @"C:\Exports\VarSel.svg";
            SvgSaveOptions svgOptions = new SvgSaveOptions
            {
                EmbedFonts = true
            };
            ws.Cells.ExportToSvg(svgPath, svgOptions);
            Console.WriteLine($"SVG exported to: {svgPath}");

            Console.WriteLine("All files generated successfully.");
        }
    }
}
```

### Ожидаемый результат

- **`VarSel.xps`** — одностраничный XPS‑документ, показывающий двойной ноль в том же шрифте, что использовался в Excel.
- **`VarSel.svg`** — файл SVG, содержащий встроенный поток шрифта; откройте его в браузере и вы увидите тот же глиф, без квадратов «отсутствующего символа».

## Распространённые подводные камни и профессиональные советы (How to Embed Fonts Effectively)

| Проблема | Почему происходит | Решение |
|----------|-------------------|---------|
| Глиф отображается как квадрат в SVG | Шрифт не был встроен (`EmbedFonts = false`) | Установите `EmbedFonts = true` в `SvgSaveOptions`. |
| Селектор варианта игнорируется | Шрифт не содержит глифа‑варианта | Выберите шрифт, явно поддерживающий селектор варианта, например **Cambria Math** или **Arial Unicode MS**. |
| Экспорт завершается ошибкой «Access denied» | Целевая папка только для чтения или не существует | Убедитесь, что папка (`C:\Exports\`) существует и процесс имеет права записи. |
| Размер файла XPS огромный | Встроены крупные файлы шрифтов без необходимости | Используйте лёгкий шрифт (например, **Calibri**), если нужны только базовые латинские символы. |

> **Профессиональный совет:** При экспорте множества листов переиспользуйте один экземпляр `SvgSaveOptions`, чтобы избежать создания дублирующих потоков шрифтов, что может раздувать размер SVG.

## Расширение решения (What If You Need More?)

- **Пакетный экспорт:** Пройдитесь в цикле по `workbook.Worksheets` и вызывайте `ExportToSvg` для каждого листа, передавая уникальное имя файла.
- **Подстановка пользовательского шрифта:** Используйте `Style.Font.Name`, чтобы принудительно задать конкретный шрифт перед экспортом. Это удобно, когда исходный workbook использует шрифт, не подходящий по лицензии.
- **Изображения более высокого разрешения:** Для растровых форматов (PNG, JPEG) можно задать `Resolution` в `ImageOrPrintOptions` — не требуется для SVG, но полезно знать, если позже понадобится генерировать PNG‑превью.

## Заключение

Мы рассмотрели **how to embed fonts** в экспорте как в XPS, так и в SVG, продемонстрировали **how to write unicode**‑символы с селекторами вариантов и показали, как **export excel to svg** с сохранением шрифтов внутри файла. Следуя этим шагам, вы избавляетесь от проблемы «отсутствующего шрифта» и гарантируете, что любой, независимо от установленных у него шрифтов, увидит именно то, что вы задумали.

Готовы к следующему вызову? Попробуйте встроить пользовательский TrueType‑шрифт, который не установлен на сервере, или поэкспериментируйте с экспортом в PDF при сохранении встроенных шрифтов. Оба пути опираются на те же принципы, которые мы изучили здесь.

Счастливого кодинга, и пусть ваши экспортируемые документы всегда выглядят пиксельно‑идеально!

{{< /blocks/products/pf/tutorial-page-section >}}
{{< /blocks/products/pf/main-container >}}
{{< /blocks/products/pf/main-wrap-class >}}
{{< blocks/products/products-backtop-button >}}