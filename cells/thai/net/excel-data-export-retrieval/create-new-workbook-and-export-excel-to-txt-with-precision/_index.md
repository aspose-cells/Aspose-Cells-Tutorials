---
category: general
date: 2026-02-15
description: สร้างเวิร์กบุ๊กใหม่และส่งออก Excel เป็นไฟล์ TXT พร้อมตั้งค่าความแม่นยำของตัวเลข
  เรียนรู้การตั้งค่าตัวเลขที่สำคัญและการจำกัดตัวเลขที่สำคัญใน C#
draft: false
keywords:
- create new workbook
- export excel to txt
- set significant digits
- limit significant digits
- set numeric precision
language: th
og_description: สร้างเวิร์กบุ๊กใหม่และส่งออกไฟล์ Excel เป็น TXT พร้อมกำหนดจำนวนหลักสำคัญเพื่อความแม่นยำของตัวเลข
  คู่มือ C# ทีละขั้นตอน.
og_title: สร้างสมุดงานใหม่ – ส่งออก Excel เป็น TXT อย่างแม่นยำ
tags:
- C#
- Aspose.Cells
- Excel automation
title: สร้างสมุดงานใหม่และส่งออก Excel เป็น TXT อย่างแม่นยำ
url: /th/net/excel-data-export-retrieval/create-new-workbook-and-export-excel-to-txt-with-precision/
---

{{< blocks/products/pf/main-wrap-class >}}
{{< blocks/products/pf/main-container >}}
{{< blocks/products/pf/tutorial-page-section >}}

# สร้าง Workbook ใหม่ – ส่งออก Excel เป็น TXT ด้วยการจัดรูปแบบตัวเลขที่แม่นยำ

เคยสงสัยไหมว่า **create new workbook** objects ใน C# และจะ dump พวกมันลงไฟล์ plain‑text ทันที? คุณไม่ได้เป็นคนเดียว ในหลายสถานการณ์ของ data‑pipeline เราต้อง **export Excel to TXT** พร้อมกับทำให้ตัวเลขอ่านง่าย ซึ่งหมายถึงการจำกัดจำนวนหลักที่ปรากฏหลังจุดทศนิยม  

ในบทแนะนำนี้ เราจะเดินผ่านกระบวนการทั้งหมด: ตั้งแต่การสร้าง workbook ใหม่, การกำหนดค่าการส่งออกให้ **sets significant digits** (หรือการจำกัด significant digits), และสุดท้ายการเขียนไฟล์ลงดิสก์. เมื่อจบคุณจะได้ snippet ที่พร้อมรันซึ่งเคารพข้อกำหนด **numeric precision** ของคุณ—ไม่มีไลบรารีเพิ่มเติม, ไม่มีเวทมนตร์.

> **Pro tip:** หากคุณกำลังใช้ Aspose.Cells อยู่แล้ว คลาสที่แสดงด้านล่างเป็นส่วนหนึ่งของไลบรารีนั้น หากคุณอยู่บนแพลตฟอร์มอื่น แนวคิดยังคงใช้ได้; เพียงเปลี่ยนการเรียก API.

---

## สิ่งที่คุณต้องการ

- .NET 6+ (โค้ดสามารถคอมไพล์บน .NET Core และ .NET Framework ได้เช่นกัน)  
- Aspose.Cells for .NET (รุ่นทดลองฟรีหรือเวอร์ชันที่มีลิขสิทธิ์) – ติดตั้งผ่าน NuGet: `dotnet add package Aspose.Cells`  
- IDE ใดก็ได้ที่คุณชอบ (Visual Studio, Rider, VS Code)  

เท่านี้เอง ไม่มีไฟล์การกำหนดค่าเพิ่มเติม ไม่มีขั้นตอนที่ซ่อนอยู่.

## ขั้นตอนที่ 1: สร้าง Workbook ใหม่

สิ่งแรกที่ต้องทำคือ **create new workbook**. คิดว่า class `Workbook` คือไฟล์ Excel ว่างเปล่าที่รอรับ sheet, cell, และข้อมูล.

```csharp
using Aspose.Cells;

class Program
{
    static void Main()
    {
        // Step 1: Instantiate a fresh workbook – this is the core of create new workbook logic
        Workbook workbook = new Workbook();

        // (Optional) Add some sample data so you can see the effect of numeric precision later
        Worksheet sheet = workbook.Worksheets[0];
        sheet.Cells["A1"].PutValue(12345.678901);
        sheet.Cells["A2"].PutValue(0.000123456);
        sheet.Cells["A3"].PutValue(Math.PI);
```

> **Why this matters:** โดยการเริ่มต้นด้วย workbook ที่สะอาดคุณจะหลีกเลี่ยงการจัดรูปแบบที่ซ่อนอยู่ซึ่งอาจรบกวนการตั้งค่า precision ในภายหลัง.

## ขั้นตอนที่ 2: กำหนดค่าตัวเลือกการบันทึกข้อความ – ตั้ง Significant Digits

ตอนนี้เราบอก Aspose.Cells ว่าเราต้องการ **significant digits** จำนวนเท่าไหร่เมื่อเขียนลงไฟล์ `.txt`. class `TxtSaveOptions` มี property `SignificantDigits` ที่ทำหน้าที่นั้น.

```csharp
        // Step 2: Prepare save options – limit numeric precision to 5 significant digits
        TxtSaveOptions txtOptions = new TxtSaveOptions
        {
            // This limits the output to 5 digits that matter, rounding the rest
            SignificantDigits = 5
        };
```

> **Explanation:** `SignificantDigits = 5` หมายความว่า exporter จะเก็บห้าหลักที่สำคัญที่สุดของตัวเลขใด ๆ ไม่ว่าจะตำแหน่งจุดทศนิยมอยู่ที่ไหน นี่เป็นวิธีที่สะดวกในการ **set numeric precision** โดยไม่ต้องจัดรูปแบบแต่ละ cell ด้วยตนเอง.

## ขั้นตอนที่ 3: บันทึก Workbook เป็นไฟล์ข้อความธรรมดา

เมื่อ workbook และ options พร้อมแล้ว เราในที่สุดก็ **export Excel to txt**. เมธอด `Save` รับพาธไฟล์และอ็อบเจ็กต์ options ที่เราตั้งค่าไว้.

```csharp
        // Step 3: Write the workbook out as a TXT file using our precision settings
        string outputPath = @"C:\Temp\numbers.txt";
        workbook.Save(outputPath, txtOptions);

        System.Console.WriteLine($"Workbook exported to {outputPath} with 5 significant digits.");
    }
}
```

การรันโปรแกรมจะสร้างไฟล์ที่มีลักษณะดังนี้:

```
12346
0.00012346
3.1416
```

สังเกตว่าตัวเลขแต่ละตัวเคารพกฎ **limit significant digits** ที่เราตั้งไว้ก่อนหน้านี้.

## ขั้นตอนที่ 4: ตรวจสอบผลลัพธ์ (ไม่บังคับแต่แนะนำ)

การเปิดไฟล์ `numbers.txt` ที่สร้างขึ้นใน editor ใดก็ง่าย แต่คุณอาจต้องการอัตโนมัติกระบวนการตรวจสอบ โดยเฉพาะใน CI pipelines.

```csharp
        // Quick verification – read back the file and print each line
        foreach (var line in System.IO.File.ReadAllLines(outputPath))
        {
            System.Console.WriteLine($"Line: {line}");
        }
```

หากคอนโซลแสดงสามบรรทัดข้างต้น คุณได้ **set significant digits** สำเร็จและการส่งออกทำงานตามที่ต้องการ.

## ข้อผิดพลาดทั่วไป & วิธีหลีกเลี่ยง

| Issue | Why it Happens | Fix |
|-------|----------------|-----|
| ตัวเลขแสดงด้วยตำแหน่งทศนิยมมากเกินไป | `SignificantDigits` ถูกปล่อยไว้เป็นค่าเริ่มต้น (0) | ตั้งค่า `SignificantDigits` อย่างชัดเจนเป็นจำนวนที่ต้องการ |
| ไฟล์ว่างถูกสร้าง | Workbook ไม่ได้รับข้อมูลใด ๆ ก่อนการบันทึก | เติมค่าให้เซลล์ **ก่อน** เรียก `Save` |
| พาธไฟล์ทำให้เกิด `UnauthorizedAccessException` | พยายามเขียนไปยังโฟลเดอร์ที่มีการป้องกัน | ใช้โฟลเดอร์ที่คุณมีสิทธิ์เขียนได้ (เช่น `C:\Temp` หรือ `%USERPROFILE%\Documents`) |
| ความแม่นยำดูผิดสำหรับตัวเลขที่มีค่ามากเล็ก | จำนวน significant digits รวมศูนย์นำหน้าหลังจุดทศนิยม | จำไว้ว่า “significant” ไม่สนใจศูนย์นำหน้า; 0.000123456 กับ 5 หลักจะกลายเป็น `0.00012346` |

## ตัวอย่างทำงานเต็มรูปแบบ (พร้อมคัดลอก‑วาง)

ด้านล่างเป็นโปรแกรมที่สมบูรณ์และเป็นอิสระ. คัดลอกไปยังโปรเจกต์คอนโซลใหม่และกด **Run**.

```csharp
using Aspose.Cells;
using System;

class Program
{
    static void Main()
    {
        // 1️⃣ Create a new workbook
        Workbook workbook = new Workbook();

        // Populate with sample numbers
        Worksheet sheet = workbook.Worksheets[0];
        sheet.Cells["A1"].PutValue(12345.678901);
        sheet.Cells["A2"].PutValue(0.000123456);
        sheet.Cells["A3"].PutValue(Math.PI);

        // 2️⃣ Set up export options – limit significant digits to 5
        TxtSaveOptions txtOptions = new TxtSaveOptions
        {
            SignificantDigits = 5
        };

        // 3️⃣ Export to TXT
        string outputPath = @"C:\Temp\numbers.txt";
        workbook.Save(outputPath, txtOptions);

        Console.WriteLine($"✅ Export completed: {outputPath}");
        Console.WriteLine("🔎 Verifying content:");
        foreach (var line in System.IO.File.ReadAllLines(outputPath))
        {
            Console.WriteLine($"   {line}");
        }
    }
}
```

**ผลลัพธ์คอนโซลที่คาดหวัง**

```
✅ Export completed: C:\Temp\numbers.txt
🔎 Verifying content:
   12346
   0.00012346
   3.1416
```

และไฟล์ `numbers.txt` จะมีสามบรรทัดที่แสดงข้างต้น.

## ขั้นตอนต่อไป: ไปไกลกว่าพื้นฐาน

- **Export other formats** – Aspose.Cells ยังรองรับ CSV, HTML, และ PDF. เปลี่ยน `TxtSaveOptions` เป็น `CsvSaveOptions` หรือ `PdfSaveOptions` ตามต้องการ.  
- **Dynamic precision** – คุณสามารถคำนวณ `SignificantDigits` ใน runtime ตามอินพุตของผู้ใช้หรือไฟล์กำหนดค่า.  
- **Multiple worksheets** – วนลูป `workbook.Worksheets` และส่งออกแต่ละ sheet ไปยังไฟล์ `.txt` ของมันเอง.  
- **Localization** – ควบคุมตัวคั่นทศนิยม (`.` vs `,`) ผ่าน `CultureInfo` หากต้องการให้ตรงกับการตั้งค่าภูมิภาค.  

ส่วนขยายทั้งหมดนี้ยังคงอิงกับแนวคิดหลักที่เราได้อธิบาย: **create new workbook**, กำหนดค่าการส่งออก, และ **set numeric precision** ให้ตรงกับความต้องการรายงานของคุณ.

## สรุป

เราได้สร้างอินสแตนซ์ **create new workbook** ใหม่, เติมข้อมูลลงไป, และสาธิตวิธี **export Excel to TXT** พร้อมกับ **setting significant digits** เพื่อจำกัดความแม่นยำของผลลัพธ์ ตัวอย่างเต็มทำงานได้ทันที, และคำอธิบายได้ครอบคลุม *เหตุผล* ของแต่ละบรรทัดเพื่อให้คุณปรับใช้ในโปรเจกต์ของคุณ.

อย่าลังเลที่จะทดลอง—เปลี่ยนค่า `SignificantDigits`, เพิ่ม sheet เพิ่มเติม, หรือสลับรูปแบบการส่งออก หากเจอปัญหา ให้ตรวจสอบเอกสาร Aspose.Cells หรือแสดงความคิดเห็นด้านล่าง. ขอให้สนุกกับการเขียนโค้ด!

![ตัวอย่างการสร้าง workbook ใหม่](/images/create-new-workbook.png "ภาพหน้าจอแสดง IDE ของ C# พร้อมโค้ดการสร้าง workbook ใหม่")

{{< /blocks/products/pf/tutorial-page-section >}}
{{< /blocks/products/pf/main-container >}}
{{< /blocks/products/pf/main-wrap-class >}}
{{< blocks/products/products-backtop-button >}}