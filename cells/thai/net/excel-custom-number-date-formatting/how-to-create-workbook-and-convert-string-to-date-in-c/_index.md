---
category: general
date: 2026-02-15
description: วิธีสร้างเวิร์กบุ๊ก, แปลงสตริงเป็นวันที่, และกำหนดรูปแบบเซลล์เป็นวันที่ด้วย
  Aspose.Cells. เรียนรู้การตั้งค่ารูปแบบตัวเลขของเซลล์และอ่านวันที่ใน Excel อย่างง่ายดาย.
draft: false
keywords:
- how to create workbook
- convert string to date
- format cell as date
- set cell number format
- read excel date
language: th
og_description: วิธีสร้างเวิร์กบุ๊ก, แปลงสตริงเป็นวันที่, และกำหนดรูปแบบเซลล์เป็นวันที่
  คู่มือขั้นตอนเต็มสำหรับการอ่านวันที่ใน Excel.
og_title: วิธีสร้าง workbook และแปลงสตริงเป็นวันที่ใน C#
tags:
- C#
- Aspose.Cells
- Excel automation
title: วิธีสร้างเวิร์กบุ๊กและแปลงสตริงเป็นวันที่ใน C#
url: /th/net/excel-custom-number-date-formatting/how-to-create-workbook-and-convert-string-to-date-in-c/
---

{{< blocks/products/pf/main-wrap-class >}}
{{< blocks/products/pf/main-container >}}
{{< blocks/products/pf/tutorial-page-section >}}

# วิธีสร้าง workbook และแปลงสตริงเป็นวันที่ใน C#

เคยสงสัย **วิธีสร้าง workbook** ที่เปลี่ยนข้อความธรรมดาอย่าง `"R3-04-01"` ให้เป็นค่า `DateTime` จริงหรือไม่? คุณไม่ได้เป็นคนเดียว—นักพัฒนาหลายคนเจอปัญหานี้เมื่อต้องดึงข้อมูลจากระบบเก่าหรือจากการป้อนของผู้ใช้ ข่าวดีคือ? ด้วยไม่กี่บรรทัดของ C# และ Aspose.Cells คุณทำได้อย่างรวดเร็วโดยไม่ต้องเขียนโค้ดแปลงเอง

ในบทแนะนำนี้เราจะเดินผ่านกระบวนการทั้งหมด: สร้าง workbook, ใส่สตริงวันที่, ใช้ **format cell as date** ที่เหมาะสม, บังคับให้เอนจิน **set cell number format**, และสุดท้าย **read excel date** กลับมาเป็น `DateTime` เมื่อเสร็จคุณจะได้โค้ดที่สามารถรันได้และนำไปใส่ในโปรเจกต์ .NET ใดก็ได้

## ข้อกำหนดเบื้องต้น

- .NET 6+ (หรือ .NET Framework 4.7.2+)
- **Aspose.Cells for .NET** NuGet package (`Install-Package Aspose.Cells`)
- ความเข้าใจพื้นฐานเกี่ยวกับไวยากรณ์ C#
- IDE เช่น Visual Studio หรือ VS Code (ใช้ได้ทุกตัว)

ไม่ต้องตั้งค่าพิเศษเพิ่มเติม—Aspose.Cells จะจัดการทุกอย่างให้เอง

## ขั้นตอน 1: วิธีสร้าง workbook – เริ่มต้นไฟล์ Excel

ก่อนอื่นเราต้องการอ็อบเจ็กต์ workbook ใหม่ คิดว่าเป็นสมุดโน๊ตเปล่าที่แต่ละ worksheet คือหน้า

```csharp
using Aspose.Cells;

 // Step 1: Create a new workbook
 var workbook = new Workbook();          // Empty workbook with one default sheet
```

*ทำไมเรื่องนี้ถึงสำคัญ:* การสร้าง workbook ให้เรามีที่เก็บเซลล์, สไตล์, และสูตร หากไม่มี workbook เราจะไม่มีที่ใส่สตริงวันที่เลย

## ขั้นตอน 2: แปลงสตริงเป็นวันที่ – ใส่ข้อความดิบ

ต่อไปเราจะใส่สตริงวันที่ดิบลงในเซลล์ **A1** ของ worksheet แรก สตริงนี้ใช้รูปแบบกำหนดเอง (`R3-04-01`) ซึ่ง Excel ไม่รู้จักโดยอัตโนมัติ

```csharp
 // Step 2: Insert a date string into cell A1 of the first worksheet
 var targetCell = workbook.Worksheets[0].Cells["A1"];
 targetCell.PutValue("R3-04-01");        // Raw text, not yet a date
```

*ทำไมเราถึงทำเช่นนี้:* `PutValue` จะเก็บข้อความตามตัวอักษร หากเราพยายามตั้งค่า `DateTime` โดยตรง รูปแบบกำหนดเองจะหายไป การเก็บเป็นข้อความทำให้เราสามารถใช้ **set cell number format** ต่อไปเพื่อบอก Excel ว่าจะตีความอย่างไร

## ขั้นตอน 3: Format cell as date – ใช้ style number 14

สไตล์วันที่ในตัวของ Excel หมายเลข 14 ตรงกับ `mm-dd-yy` การกำหนดสไตล์นี้ทำให้เราแจ้งเอนจินว่า “ให้ถือเนื้อหาในเซลล์นี้เป็นวันที่”

```csharp
 // Step 3: Apply a date number format (style number 14) to the cell
 targetCell.SetStyle(new Style { Number = 14 });
```

*สิ่งที่เกิดขึ้นเบื้องหลัง:* คุณสมบัติ `Number` จะเชื่อมกับ ID ของรูปแบบตัวเลขภายใน Excel เมื่อ workbook คำนวณใหม่ Excel จะพยายามแปลงข้อความเป็นวันที่แบบ serial ตามรูปแบบที่กำหนด

## ขั้นตอน 4: Set cell number format – บังคับให้คำนวณใหม่

Excel จะไม่แปลงข้อความโดยอัตโนมัติจนกว่าเราจะสั่งให้ประเมินสูตร (หรือในกรณีนี้ให้ตีความเซลล์ใหม่) การเรียก `CalculateFormula` จะทำให้เกิดการแปลงนั้น

```csharp
 // Step 4: Recalculate any formulas so the cell value is interpreted as a date
 workbook.CalculateFormula();
```

*เคล็ดลับ:* หากคุณทำงานกับหลายเซลล์ สามารถเรียก `CalculateFormula` ครั้งเดียวหลังจากทำการจัดรูปแบบทั้งหมดเสร็จ—จะประหยัดเวลาไม่กี่มิลลิวินาที

## ขั้นตอน 5: Read Excel date – ดึงค่า DateTime

สุดท้ายเราจะดึงค่าการแสดงผล `DateTime` ออกจากเซลล์ Aspose.Cells ให้เข้าถึงได้ผ่าน `DateTimeValue`

```csharp
 // Step 5: Retrieve the DateTime representation and display it
 Console.WriteLine(targetCell.DateTimeValue);
```

**ผลลัพธ์ที่คาดหวัง (สมมติใช้ปฏิทิน Gregorian เริ่มต้น):**

```
2023-04-01 00:00:00
```

สังเกตว่า prefix `"R3-"` จะถูกละเว้นเพราะตัวแยกวันที่ของ Excel ให้ความสำคัญกับส่วนตัวเลขเมื่อสไตล์เป็นวันที่ หากสตริงของคุณมี prefix อื่น ๆ คุณอาจต้องทำการพรี‑โปรเซสก่อน แต่สำหรับรูปแบบเก่าหลายแบบวิธีนี้ทำงานได้อย่างสมบูรณ์

## ตัวอย่างทำงานเต็มรูปแบบ

รวมทุกขั้นตอนเข้าด้วยกัน นี่คือโปรแกรมที่พร้อมรันเต็มรูปแบบ:

```csharp
using System;
using Aspose.Cells;

class Program
{
    static void Main()
    {
        // Step 1: Create a new workbook
        var workbook = new Workbook();

        // Step 2: Insert a date string into cell A1 of the first worksheet
        var targetCell = workbook.Worksheets[0].Cells["A1"];
        targetCell.PutValue("R3-04-01");

        // Step 3: Apply a date number format (style number 14) to the cell
        targetCell.SetStyle(new Style { Number = 14 });

        // Step 4: Recalculate any formulas so the cell value is interpreted as a date
        workbook.CalculateFormula();

        // Step 5: Retrieve the DateTime representation and display it
        Console.WriteLine(targetCell.DateTimeValue);
    }
}
```

บันทึกไฟล์นี้เป็น `Program.cs`, เรียกคืนแพคเกจ Aspose.Cells, แล้วรัน `dotnet run` คุณจะเห็น `DateTime` ที่จัดรูปแบบแล้วแสดงบนคอนโซล

## ความแปรผันทั่วไป & กรณีขอบ

### สตริงวันที่ต่างกัน

หากข้อมูลต้นทางของคุณเป็น `"2023/04/01"` หรือ `"01‑Apr‑2023"` คุณยังคงใช้กระบวนการเดียวกัน—เพียงเปลี่ยนคุณสมบัติ **Number** ให้ตรงกับรูปแบบ (เช่น `Number = 15` สำหรับ `d-mmm-yy`)

### รูปแบบตามโลคัล

Excel เคารพการตั้งค่าโลคัลของ workbook เพื่อบังคับให้แปลงแบบสไตล์ US ให้ตั้งค่าภูมิภาคของ workbook:

```csharp
workbook.Settings.CultureInfo = new System.Globalization.CultureInfo("en-US");
```

### เมื่อสตริงไม่สามารถระบุได้

บางครั้ง Excel ไม่สามารถสรุปวันที่ได้ (เช่น `"R3-13-40"`). ในกรณีนั้นให้ทำการพรี‑โปรเซสสตริงก่อน:

```csharp
string raw = "R3-04-01";
string cleaned = raw.Replace("R3-", "");   // Remove the prefix
targetCell.PutValue(cleaned);
```

จากนั้นจึงใช้รูปแบบตัวเลขเดียวกันต่อไป

## เคล็ดลับระดับมืออาชีพ & สิ่งที่ควรระวัง

- **Pro tip:** ใช้ `StyleFlag` เพื่อแก้ไขเฉพาะรูปแบบตัวเลขโดยไม่กระทบสไตล์อื่น ๆ  
  ```csharp
  var style = targetCell.GetStyle();
  style.Number = 14;
  var flag = new StyleFlag { Number = true };
  targetCell.SetStyle(style, flag);
  ```
- **ระวัง:** การเขียนทับสไตล์ที่มีอยู่แล้วบนเซลล์ที่มีเส้นขอบหรือฟอนต์ `StyleFlag` จะช่วยป้องกันปัญหานี้
- **หมายเหตุเรื่องประสิทธิภาพ:** หากคุณประมวลผลหลายพันแถว ควรทำ batch การเรียก `CalculateFormula` หลังจากอัปเดตทั้งหมดเสร็จ; การเรียกต่อแถวจะเพิ่มภาระที่ไม่จำเป็น

## สรุป

คุณได้เรียนรู้ **วิธีสร้าง workbook**, **แปลงสตริงเป็นวันที่**, **format cell as date**, **set cell number format**, และสุดท้าย **read excel date** กลับเป็น `DateTime` แล้ว รูปแบบนี้ง่ายมาก: ใส่ข้อความดิบ, ใช้สไตล์วันที่, บังคับให้คำนวณใหม่, แล้วดึงค่าออกมา

จากนี้คุณสามารถขยายตรรกะไปยังคอลัมน์ทั้งหมด, นำเข้า CSV, หรือแม้กระทั่งสร้างรายงานที่แปลงสตริงวันที่แบบเก่าให้เป็นวันที่ของ Excel อัตโนมัติ

พร้อมจะก้าวต่อ? ลองใช้รูปแบบตัวเลขกำหนดเอง (`Number = 22`) เพื่อแสดงวันที่เป็น `yyyy-mm-dd` หรือสำรวจยูทิลิตี้ `DateTimeConversion` ของ Aspose.Cells สำหรับกรณีซับซ้อนอื่น ๆ

เขียนโค้ดให้สนุก! 🚀

{{< /blocks/products/pf/tutorial-page-section >}}
{{< /blocks/products/pf/main-container >}}
{{< /blocks/products/pf/main-wrap-class >}}
{{< blocks/products/products-backtop-button >}}