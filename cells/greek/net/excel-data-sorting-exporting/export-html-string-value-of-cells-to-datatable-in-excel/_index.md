---
"description": "Μάθετε πώς να εξάγετε τιμές συμβολοσειρών HTML από κελιά του Excel σε έναν Πίνακα Δεδομένων χρησιμοποιώντας το Aspose.Cells για .NET σε ένα απλό βήμα προς βήμα σεμινάριο."
"linktitle": "Εξαγωγή τιμής συμβολοσειράς HTML κελιών σε πίνακα δεδομένων στο Excel"
"second_title": "API επεξεργασίας Excel Aspose.Cells .NET"
"title": "Εξαγωγή τιμής συμβολοσειράς HTML κελιών σε πίνακα δεδομένων στο Excel"
"url": "/el/net/excel-data-sorting-exporting/export-html-string-value-of-cells-to-datatable-in-excel/"
"weight": 11
---

{{< blocks/products/pf/main-wrap-class >}}

{{< blocks/products/pf/main-container >}}

{{< blocks/products/pf/tutorial-page-section >}}

# Εξαγωγή τιμής συμβολοσειράς HTML κελιών σε πίνακα δεδομένων στο Excel

## Εισαγωγή

Όταν εργάζεστε με αρχεία Excel σε περιβάλλον .NET, μπορεί να χρειαστεί να εξαγάγετε πληροφορίες από κελιά, όχι μόνο ως απλό κείμενο αλλά και ως συμβολοσειρές HTML. Αυτό μπορεί να είναι αρκετά χρήσιμο όταν έχετε να κάνετε με δεδομένα εμπλουτισμένου κειμένου ή όταν θέλετε να διατηρήσετε τη μορφοποίηση. Σε αυτόν τον οδηγό, θα σας καθοδηγήσω στην εξαγωγή της τιμής συμβολοσειράς HTML των κελιών σε έναν Πίνακα Δεδομένων χρησιμοποιώντας το Aspose.Cells για .NET. 

## Προαπαιτούμενα

Πριν εμβαθύνουμε στον κώδικα, ας βεβαιωθούμε ότι έχετε όλα όσα χρειάζεστε στη διάθεσή σας. Ακολουθεί μια γρήγορη λίστα ελέγχου:

1. Βασικές γνώσεις C# και .NET: Πριν ξεκινήσετε να γράφετε κώδικα, βεβαιωθείτε ότι είστε εξοικειωμένοι με τον προγραμματισμό C# και τα βασικά του .NET framework.
2. Aspose.Cells για .NET: Εάν δεν το έχετε κάνει ήδη, πρέπει να εγκαταστήσετε το Aspose.Cells για .NET. Μπορείτε να κατεβάσετε μια δωρεάν δοκιμαστική έκδοση από [εδώ](https://releases.aspose.com/).
3. Visual Studio ή IDE της επιλογής σας: Ρυθμίστε το περιβάλλον σας για να γράφετε κώδικα C#. Το Visual Studio συνιστάται για το ευρύ φάσμα χαρακτηριστικών και την ευκολία χρήσης του.
4. Δείγμα αρχείου Excel: Θα χρειαστείτε ένα δείγμα αρχείου Excel (`sampleExportTableAsHtmlString.xlsx`) για να εργαστείτε. Βεβαιωθείτε ότι βρίσκεται σε έναν κατάλογο στον οποίο είναι δυνατή η πρόσβαση.
5. NuGet Package Manager: Βεβαιωθείτε ότι έχετε πρόσβαση στο NuGet Package Manager στο έργο σας για να προσθέσετε εύκολα τη βιβλιοθήκη Aspose.Cells.

Έχοντας αυτές τις προϋποθέσεις υπό έλεγχο, ας ξεκινήσουμε λίγο τον προγραμματισμό!

## Εισαγωγή πακέτων

Πριν ξεκινήσουμε να εργαζόμαστε με το Aspose.Cells, πρέπει να εισαγάγουμε τα απαραίτητα πακέτα. Αυτό συνήθως περιλαμβάνει την προσθήκη του πακέτου Aspose.Cells NuGet στο έργο σας. Δείτε πώς μπορείτε να το κάνετε:

### Άνοιγμα του διαχειριστή πακέτων NuGet

Στο Visual Studio, κάντε δεξί κλικ στο έργο σας στην Εξερεύνηση λύσεων και επιλέξτε Διαχείριση πακέτων NuGet.

### Αναζήτηση για Aspose.Cells

Στον Διαχειριστή Πακέτων NuGet, πληκτρολογήστε `Aspose.Cells` στη γραμμή αναζήτησης.

### Εγκατάσταση του πακέτου

Μόλις βρείτε το Aspose.Cells, κάντε κλικ στο κουμπί Εγκατάσταση. Αυτό θα προσθέσει τη βιβλιοθήκη στο έργο σας και θα σας επιτρέψει να την εισαγάγετε στον κώδικά σας.

### Εισαγωγή του χώρου ονομάτων

Προσθέστε τα ακόλουθα χρησιμοποιώντας την οδηγία στην αρχή του αρχείου κώδικά σας:

```csharp
using System;
using System.IO;
using Aspose.Cells;
using System.Data;
```

Τώρα που έχουμε ρυθμίσει τα πάντα, ας δούμε βήμα προς βήμα τη διαδικασία εξαγωγής τιμών συμβολοσειρών HTML από ένα αρχείο Excel σε έναν Πίνακα Δεδομένων. 

## Βήμα 1: Ορίστε τον κατάλογο πηγών

Θα ξεκινήσετε ορίζοντας τον κατάλογο όπου αποθηκεύεται το δείγμα αρχείου Excel. Αυτό είναι κρίσιμο, καθώς υποδεικνύει στην εφαρμογή σας πού να βρει το αρχείο. Ακολουθεί ο κώδικας για αυτό:

```csharp
string sourceDir = "Your Document Directory";
```

Φροντίστε να αντικαταστήσετε `"Your Document Directory"` με την πραγματική διαδρομή προς το αρχείο Excel σας.

## Βήμα 2: Φόρτωση του δείγματος αρχείου Excel

Το επόμενο βήμα είναι να φορτώσετε το βιβλίο εργασίας του Excel. Θα χρησιμοποιήσετε το `Workbook` κλάση από το Aspose.Cells για να το κάνετε αυτό. Δείτε πώς μπορείτε να φορτώσετε το αρχείο:

```csharp
Workbook wb = new Workbook(sourceDir + "sampleExportTableAsHtmlString.xlsx");
```

Αυτή η απλή γραμμή κώδικα αρχικοποιεί το βιβλίο εργασίας και φορτώνει το καθορισμένο αρχείο Excel.

## Βήμα 3: Πρόσβαση στο πρώτο φύλλο εργασίας

Μόλις φορτωθεί το βιβλίο εργασίας, θα πρέπει να αποκτήσετε πρόσβαση στο συγκεκριμένο φύλλο εργασίας που περιέχει τα δεδομένα που σας ενδιαφέρουν. Γενικά, θα ξεκινήσετε με το πρώτο φύλλο εργασίας:

```csharp
Worksheet ws = wb.Worksheets[0];
```

Εδώ, εργαζόμαστε με το πρώτο φύλλο εργασίας (ευρετήριο 0). Βεβαιωθείτε ότι τα δεδομένα σας βρίσκονται στο σωστό φύλλο.

## Βήμα 4: Καθορισμός επιλογών εξαγωγής πίνακα

Για να ελέγξετε τον τρόπο εξαγωγής των δεδομένων, πρέπει να ρυθμίσετε `ExportTableOptions`Σε αυτήν την περίπτωση, θέλετε να βεβαιωθείτε ότι τα ονόματα στηλών δεν εξάγονται και θέλετε τα δεδομένα κελιών να εξαχθούν ως συμβολοσειρές HTML:

```csharp
ExportTableOptions opts = new ExportTableOptions();
opts.ExportColumnName = false;
opts.ExportAsHtmlString = true;
```

Αυτή η ρύθμιση παραμέτρων σάς επιτρέπει να διατηρείτε την πλούσια μορφοποίηση των δεδομένων κελιών σας κατά την εξαγωγή.

## Βήμα 5: Εξαγωγή κελιών σε DataTable

Τώρα έρχεται το κρίσιμο μέρος όπου εξάγετε στην πραγματικότητα τα δεδομένα. Χρησιμοποιώντας το `ExportDataTable` με τη μέθοδο, μπορείτε να ανασύρετε τα δεδομένα από το φύλλο εργασίας σε ένα `DataTable`Δείτε πώς μπορείτε να το κάνετε αυτό:

```csharp
DataTable dt = ws.Cells.ExportDataTable(0, 0, 3, 3, opts);
```

Αυτός ο κώδικας εξάγει μια καθορισμένη περιοχή κελιών (από τη γραμμή 0, στήλη 0 έως τη γραμμή 3, στήλη 3) σε έναν Πίνακα Δεδομένων χρησιμοποιώντας τις επιλογές που καθορίστηκαν προηγουμένως.

## Βήμα 6: Εκτύπωση της τιμής συμβολοσειράς HTML

Τέλος, ας εκτυπώσουμε την τιμή συμβολοσειράς HTML από ένα συγκεκριμένο κελί στον Πίνακα Δεδομένων για να δούμε τι έχουμε καταφέρει να εξαγάγουμε. Για παράδειγμα, αν θέλετε να εκτυπώσετε την τιμή από την τρίτη γραμμή και τη δεύτερη στήλη, θα πρέπει να κάνετε τα εξής:

```csharp
Console.WriteLine(dt.Rows[2][1].ToString());
```

Αυτή η γραμμή εκτυπώνει την επιθυμητή συμβολοσειρά HTML από τον Πίνακα Δεδομένων στην κονσόλα. 

## Σύναψη 

Και να το! Εξάγατε με επιτυχία τιμές συμβολοσειρών HTML από κελιά σε ένα αρχείο Excel σε έναν Πίνακα Δεδομένων χρησιμοποιώντας το Aspose.Cells για .NET. Αυτή η δυνατότητα όχι μόνο εμπλουτίζει τις δεξιότητές σας στον χειρισμό δεδομένων, αλλά διευρύνει και τις επιλογές σας όταν ασχολείστε με μορφοποιημένο περιεχόμενο απευθείας από αρχεία Excel. 

## Συχνές ερωτήσεις

### Μπορώ να χρησιμοποιήσω το Aspose.Cells για άλλες μορφές αρχείων εκτός από το Excel;  
Ναι, το Aspose.Cells προορίζεται κυρίως για το Excel, αλλά το Aspose προσφέρει και άλλες βιβλιοθήκες για διαφορετικές μορφές.

### Χρειάζομαι άδεια χρήσης για το Aspose.Cells;  
Ναι, απαιτείται έγκυρη άδεια για χρήση στην παραγωγή. Μπορείτε να λάβετε μια προσωρινή άδεια. [εδώ](https://purchase.aspose.com/temporary-license/).

### Τι γίνεται αν το αρχείο Excel μου περιέχει τύπους; Θα εξαχθούν σωστά;  
Ναι, το Aspose.Cells μπορεί να χειριστεί τύπους και κατά την εξαγωγή, θα αξιολογηθούν στις τιμές που προκύπτουν.

### Είναι δυνατόν να αλλάξω τις επιλογές εξαγωγής;  
Απολύτως! Μπορείτε να το προσαρμόσετε! `ExportTableOptions` για να ταιριάζει στις συγκεκριμένες ανάγκες σας.

### Πού μπορώ να βρω πιο λεπτομερή τεκμηρίωση για το Aspose.Cells;  
Μπορείτε να βρείτε εκτενή τεκμηρίωση [εδώ](https://reference.aspose.com/cells/net/).

{{< /blocks/products/pf/tutorial-page-section >}}

{{< /blocks/products/pf/main-container >}}

{{< /blocks/products/pf/main-wrap-class >}}

{{< blocks/products/products-backtop-button >}}