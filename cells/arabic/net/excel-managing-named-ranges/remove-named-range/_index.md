---
"description": "تعرف على كيفية إزالة النطاقات المسماة في Excel باستخدام Aspose.Cells لـ .NET مع تعليمات مفصلة خطوة بخطوة."
"linktitle": "إزالة النطاق المسمى في Excel"
"second_title": "واجهة برمجة تطبيقات معالجة Excel Aspose.Cells .NET"
"title": "إزالة النطاق المسمى في Excel"
"url": "/ar/net/excel-managing-named-ranges/remove-named-range/"
"weight": 11
---

{{< blocks/products/pf/main-wrap-class >}}

{{< blocks/products/pf/main-container >}}

{{< blocks/products/pf/tutorial-page-section >}}

# إزالة النطاق المسمى في Excel

## مقدمة
أصبح برنامج إكسل أداةً أساسيةً في إدارة البيانات وتحليلها للعديد من الأفراد والمؤسسات. سواءً كنت محلل بيانات محترفًا أو ببساطة شخصًا يستمتع بتنظيم بياناته، فإن إتقان إكسل أمرٌ أساسي. اليوم، سنتناول ميزةً محددةً وفعّالة: إزالة النطاقات المُسمّاة باستخدام Aspose.Cells لـ .NET. سيرشدك هذا الدليل إلى خطوات تحقيق ذلك بفعالية. لذا، هيا بنا نبدأ!

## المتطلبات الأساسية

قبل أن ننتقل إلى الترميز الفعلي، هناك بعض الأشياء التي ستحتاج إلى وضعها في مكانها:

### إعداد بيئة .NET

للعمل مع Aspose.Cells لـ .NET بسلاسة، تأكد من أن لديك ما يلي:

1. Visual Studio: قم بتنزيل وتثبيت Visual Studio (إصدار Community جيد تمامًا) والذي يمكنك العثور عليه على [موقع ويب Visual Studio](https://visualstudio.microsoft.com/).
2. إطار عمل .NET: تأكد من استخدام الإصدار المناسب من إطار عمل .NET. يدعم Aspose.Cells إطار عمل .NET 4.0 والإصدارات الأحدث.
3. مكتبة Aspose.Cells: عليك تنزيل مكتبة Aspose.Cells for .NET والرجوع إليها في تطبيقك. يمكنك العثور على الحزمة القابلة للتنزيل. [هنا](https://releases.aspose.com/cells/net/).

### فهم أساسي للغة C#

ستحتاج إلى فهم أساسيات برمجة C#. سيساعدك هذا على فهم مقتطفات التعليمات البرمجية التي سنناقشها.

### الوصول إلى ملفات Excel

تأكد من وجود ملف Excel جاهز للتجربة. إذا لم يكن لديك، يمكنك إنشاء ملف سريع باستخدام Microsoft Excel.

## استيراد الحزم

بعد أن غطينا المتطلبات الأساسية، لنستورد الحزم التي نحتاجها في مشروعنا. افتح Visual Studio وأنشئ تطبيق وحدة تحكم جديدًا. ثم أضف مساحة الاسم التالية إلى برنامجك:

```csharp
using System;
using System.IO;
using Aspose.Cells;
using System.Drawing;
```

يتيح لك هذا الإعداد الاستفادة من الوظائف التي يوفرها Aspose.Cells للتعامل مع جداول بيانات Excel بسهولة.

## الخطوة 1: إعداد دليل الإخراج

أولاً، علينا تحديد مكان حفظ ملف الإخراج. هذا أمر بالغ الأهمية لتجنب أي لبس لاحقاً حول مكان حفظ ملفاتك.

```csharp
// دليل الإخراج
string outputDir = "Your Document Directory Here\\";
```

يستبدل `"Your Document Directory Here\\"` مع المسار الموجود على جهاز الكمبيوتر الخاص بك حيث تريد حفظ ملفك.

## الخطوة 2: إنشاء مصنف جديد

كيف نبدأ من الصفر؟ بإنشاء دفتر عمل جديد، بالطبع! سيكون هذا الدفتر بمثابة لوحة بيضاء.

```csharp
// إنشاء مصنف جديد.
Workbook workbook = new Workbook();
```

يؤدي هذا السطر من التعليمات البرمجية إلى إنشاء مصنف جديد يمكننا التعامل معه.

## الخطوة 3: الوصول إلى مجموعة أوراق العمل

يتكون كل مصنف من ورقة عمل واحدة أو أكثر. للعمل ضمن ورقة عمل محددة، نحتاج إلى الوصول إلى هذه المجموعة.

```csharp
// احصل على جميع أوراق العمل الموجودة في الكتاب.
WorksheetCollection worksheets = workbook.Worksheets;
```

هنا، قمنا باسترجاع جميع أوراق العمل المتوفرة في مصنفنا الجديد.

## الخطوة 4: اختيار ورقة العمل الأولى

بعد ذلك، نريد العمل داخل ورقة العمل الأولى - نقطة البداية الافتراضية في العديد من الحالات.

```csharp
// احصل على ورقة العمل الأولى في مجموعة أوراق العمل.
Worksheet worksheet = workbook.Worksheets[0];
```

يتيح لنا مقتطف التعليمات البرمجية هذا تحديد ورقة العمل الأولى بسهولة.

## الخطوة 5: إنشاء نطاقات مسماة

الآن، لنُنشئ نطاقًا مُسمّى، وهو جزء أساسي من هذا البرنامج التعليمي. سيُتيح لنا هذا توضيح كيفية إزالة نطاق مُسمّى لاحقًا.

```csharp
// إنشاء نطاق من الخلايا.
Range range1 = worksheet.Cells.CreateRange("E12", "I12");

// قم بتسمية النطاق.
range1.Name = "FirstRange";
```

هنا، نقوم بتعريف نطاق من الخلايا E12 إلى I12 ونسميه "FirstRange".

## الخطوة 6: تنسيق النطاق المسمى

لإظهار مدى تنوع Aspose.Cells، دعنا نضيف بعض التنسيق إلى النطاق المسمى لدينا.

```csharp
// تعيين حدود المخطط التفصيلي للنطاق.
range1.SetOutlineBorder(BorderType.TopBorder, CellBorderType.Medium, Color.FromArgb(0, 0, 128));
range1.SetOutlineBorder(BorderType.BottomBorder, CellBorderType.Medium, Color.FromArgb(0, 0, 128));
range1.SetOutlineBorder(BorderType.LeftBorder, CellBorderType.Medium, Color.FromArgb(0, 0, 128));
range1.SetOutlineBorder(BorderType.RightBorder, CellBorderType.Medium, Color.FromArgb(0, 0, 128));
```

نحن نضيف حدودًا متوسطة الحجم باللون الأزرق البحري حول مجموعتنا لجعلها جذابة بصريًا.

## الخطوة 7: إدخال البيانات في النطاق

بعد ذلك، يمكننا ملء خلايانا ببعض البيانات لجعلها وظيفية.

```csharp
// قم بإدخال بعض البيانات مع بعض التنسيقات في عدد قليل من الخلايا في النطاق.
range1[0, 0].PutValue("Test");            
range1[0, 4].PutValue(123);
```

في هذه الخطوة، وضعنا كلمة "اختبار" في الخلية E12 والرقم 123 في الخلية I12.

## الخطوة 8: إنشاء نطاق مسمى آخر

ولتوضيح وجهة نظرنا بشكل أكبر، سنقوم بإنشاء نطاق آخر باسم مشابه للنطاق الأول.

```csharp
// إنشاء نطاق آخر من الخلايا.
Range range2 = worksheet.Cells.CreateRange("B3", "F3");

// قم بتسمية النطاق.
range2.Name = "SecondRange";
```

لدينا الآن نطاق آخر يسمى "SecondRange" متاحًا للاستخدام.

## الخطوة 9: نسخ النطاق الأول إلى النطاق الثاني

دعونا نوضح كيفية استخدام النطاق الثاني عن طريق نسخ البيانات من النطاق الأول.

```csharp
// انسخ النطاق الأول إلى النطاق الثاني.
range2.Copy(range1);
```

من خلال هذه الخطوة، قمنا بتكرار البيانات من "FirstRange" إلى "SecondRange" بشكل فعال.

## الخطوة 10: إزالة النطاق المسمى

الآن، نصل إلى أهم ما في درسنا: إزالة النطاق المُسمّى. هنا تكمن نقطة البداية.

```csharp
// قم بإزالة النطاق المسمى سابقًا (range1) مع محتوياته.
worksheet.Cells.ClearRange(range1.FirstRow, range1.FirstColumn, range1.FirstRow + range1.RowCount - 1, range1.FirstColumn + range1.ColumnCount - 1);
```

يقوم هذا الخط بمسح محتويات النطاق الذي نريد إزالته، مما يضمن عدم ترك أي أثر!

## الخطوة 11: حذف النطاق المسمى من ورقة العمل

الخطوة النهائية المهمة هي إزالة النطاق المسمى من مجموعة أسماء ورقة العمل.

```csharp
worksheets.Names.RemoveAt(0);
```

سيؤدي هذا إلى إزالة النطاق المسمى "FirstRange" من المصنف بشكل فعال.

## الخطوة 12: حفظ المصنف

وأخيرا وليس آخرا، دعونا نحفظ عملنا. 

```csharp
// احفظ ملف Excel.
workbook.Save(outputDir + "outputRemoveNamedRange.xlsx");
```

يحفظ هذا الأمر المصنف الخاص بك بالتغييرات التي أجريناها - وهذا هو المكان الذي يتم فيه حفظ كل عملك الشاق!

## الخطوة 13: تأكيد التنفيذ الناجح

ولإنهاء الأمور بشكل أنيق، قد ترغب في إخراج رسالة نجاح إلى وحدة التحكم.

```csharp
Console.WriteLine("RemoveNamedRange executed successfully.");
```

هذا يخطرك بأن العملية بأكملها تمت دون أية عوائق!

## خاتمة

باتباع هذا الدليل، ستتعلم كيفية التعامل مع النطاقات المُسمّاة في Excel باستخدام Aspose.Cells لـ .NET. لقد أنشأتَ النطاقات، وملأتَها بالبيانات، ونسختَ محتوياتها، وأزلتَها في النهاية مع ضمان بقاء ملف Excel مُنظّمًا ونظيفًا. يزدهر Excel، تمامًا مثل مقهى صاخب، بالتنظيم. لذا، سواء كنت تُدير بيانات تقرير أو تُحسّن ميزانية ميزانيتك الشخصية، فإن إتقان النطاقات المُسمّاة يُمكن أن يُساعدك في ابتكار حلول فعّالة. 

## الأسئلة الشائعة

### ما هو Aspose.Cells؟
Aspose.Cells هي مكتبة .NET مصممة للتعامل مع ملفات Excel برمجيًا.

### هل يمكنني إزالة نطاقات متعددة مرة واحدة؟
نعم، يمكنك التنقل عبر مجموعة النطاقات المسماة وإزالتها حسب الحاجة.

### هل هناك نسخة تجريبية متاحة؟
نعم، يمكنك تنزيل نسخة تجريبية مجانية من Aspose.Cells [هنا](https://releases.aspose.com/).

### ما هي لغات البرمجة التي يدعمها Aspose.Cells؟
إنه يدعم بشكل أساسي لغات .NET مثل C# و VB.NET، وغيرها.

### أين يمكنني الحصول على الدعم إذا واجهت مشاكل؟
يمكنك زيارة [منتدى دعم Aspose](https://forum.aspose.com/c/cells/9) للحصول على المساعدة بشأن أي استفسارات.

{{< /blocks/products/pf/tutorial-page-section >}}

{{< /blocks/products/pf/main-container >}}

{{< /blocks/products/pf/main-wrap-class >}}

{{< blocks/products/products-backtop-button >}}