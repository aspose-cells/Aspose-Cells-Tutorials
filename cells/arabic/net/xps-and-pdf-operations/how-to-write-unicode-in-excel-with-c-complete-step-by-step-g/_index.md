---
category: general
date: 2026-02-28
description: تعلم كيفية كتابة Unicode في Excel باستخدام C#. يوضح هذا الدرس أيضًا كيفية
  إضافة الرموز التعبيرية في Excel، وكيفية إنشاء ملفات Excel، وكيفية تحويل Excel إلى
  XPS.
draft: false
keywords:
- how to write unicode
- how to create excel
- add emoji in excel
- convert excel to xps
- add unicode emoji
language: ar
og_description: اكتشف كيفية كتابة Unicode في Excel، وإضافة الرموز التعبيرية في خلايا
  Excel، وإنشاء دفاتر عمل Excel، وتحويل Excel إلى XPS باستخدام C#. كود ونصائح خطوة
  بخطوة.
og_title: كيفية كتابة Unicode في Excel باستخدام C# – دليل برمجي كامل
tags:
- Aspose.Cells
- C#
- Excel automation
title: كيفية كتابة Unicode في Excel باستخدام C# – دليل خطوة بخطوة كامل
url: /ar/net/xps-and-pdf-operations/how-to-write-unicode-in-excel-with-c-complete-step-by-step-g/
---

{{< blocks/products/pf/main-wrap-class >}}
{{< blocks/products/pf/main-container >}}
{{< blocks/products/pf/tutorial-page-section >}}

# كيف تكتب Unicode في Excel باستخدام C# – دليل خطوة‑بخطوة كامل

هل تساءلت يومًا **كيف تكتب Unicode** في ورقة عمل Excel دون أن تفقد أعصابك؟ لست وحدك. المطورون يحتاجون باستمرار إلى إدراج الإيموجي، الرموز الخاصة، أو الأحرف الخاصة بلغات معينة في الجداول، وغالبًا ما يفشل الأسلوب التقليدي `Cell.Value = "😀"` بسبب عدم توافق الترميز.  

في هذا الدليل سنحل هذه المشكلة تمامًا، نوضح **كيفية إنشاء ملفات Excel** برمجيًا، نُظهر **إضافة إيموجي في خلايا Excel**، ونختتم بمثال نظيف لـ **تحويل Excel إلى XPS**. في النهاية ستحصل على مقتطف C# جاهز للتنفيذ يكتب إيموجي رجل (👨‍) في الخلية `A1` ويحفظ المصنف بالكامل كملف XPS.

## ما الذي ستحتاجه

- **.NET 6+** (أو .NET Framework 4.6+). أي نسخة حديثة من الـ runtime تعمل؛ الكود يستخدم فقط ميزات C# القياسية.
- **Aspose.Cells for .NET** – المكتبة التي تسمح لنا بالتعامل مع ملفات Excel دون الحاجة لتثبيت Office. احصل عليها من NuGet (`Install-Package Aspose.Cells`).
- بيئة تطوير متكاملة (Visual Studio، Rider، أو VS Code).  
- لا تحتاج إلى خبرة سابقة في Unicode – سنشرح نقاط الكود.

> **Pro tip:** إذا كان لديك مشروع بالفعل يضم Aspose.Cells، يمكنك وضع الكود مباشرةً؛ وإلا أنشئ تطبيقًا جديدًا من نوع console وأضف حزمة NuGet أولًا.

## الخطوة 1: إعداد المشروع واستيراد المساحات الاسمية

أولًا، أنشئ تطبيق console جديد واستورد المساحات الاسمية الضرورية. هذا هو الأساس لـ **كيفية إنشاء ملفات Excel** من الصفر.

```csharp
using System;
using Aspose.Cells;          // Core Excel API
using Aspose.Cells.Drawing; // Required for XPS options (optional but clearer)

namespace UnicodeExcelDemo
{
    class Program
    {
        static void Main(string[] args)
        {
            // The rest of the tutorial lives here
        }
    }
}
```

*لماذا هذا مهم:* `Aspose.Cells` يزودنا بالفئات `Workbook`، `Worksheet`، و `XpsSaveOptions` التي سنستخدمها. استيرادها مسبقًا يبقي الكود اللاحق منظمًا.

## الخطوة 2: إنشاء مصنف جديد والوصول إلى الورقة الأولى

الآن سنجيب على **كيفية إنشاء كائنات Excel** في الذاكرة. تخيل المصنف كدفتر فارغ؛ الورقة الأولى هي الصفحة الأولى.

```csharp
// Step 2: Initialize a fresh workbook
Workbook workbook = new Workbook();

// Grab the first (and default) worksheet – index 0
Worksheet worksheet = workbook.Worksheets[0];
```

*شرح:* مُنشئ `Workbook` يُنشئ ملف Excel فارغ يحتوي على ورقة واحدة تلقائيًا. الوصول إلى `Worksheets[0]` آمن لأن Aspose دائمًا يخلق ورقة واحدة على الأقل.

## الخطوة 3: كتابة إيموجي Unicode (رجل + Variation Selector‑16) في الخلية A1

هذا هو جوهر **كيفية كتابة Unicode** بشكل صحيح. نقاط الكود Unicode تُكتب في C# بصيغة `\u{...}` (متاحة منذ C# 10). الإيموجي الذي نريد هو مكوّن من جزأين:

1. `U+1F468` – الحرف الأساسي “MAN”.
2. `U+FE0F` – Variation Selector‑16، الذي يفرض عرض الإيموجي.

```csharp
// Step 3: Insert the emoji into cell A1
// \u{1F468} = 👨  (MAN)
// \u{FE0F} = Variation Selector‑16 (forces emoji style)
worksheet.Cells["A1"].PutValue("\u{1F468}\u{FE0F}");
```

*لماذا نحتاج Variation Selector؟* بدون `FE0F` قد يعرض بعض العارضين الحرف كنص عادي بدلاً من الإيموجي الملون. إضافة هذا المحدد يضمن “نمط الإيموجي” في معظم المنصات، وهو أمر أساسي عندما **تضيف إيموجي Unicode** إلى Excel.

## الخطوة 4: إعداد خيارات حفظ XPS (اختياري لكن مُستحسن)

إذا كنت تخطط لـ **تحويل Excel إلى XPS**، يمكنك ضبط الإخراج باستخدام `XpsSaveOptions`. الخيارات الافتراضية تُنتج تحويلًا دقيقًا، لكننا سننشئ الكائن صراحةً لجعل الكود واضحًا وقابلًا للتوسيع.

```csharp
// Step 4: Set up XPS save options (default configuration)
XpsSaveOptions xpsOptions = new XpsSaveOptions();
```

*ملاحظة:* يمكنك تخصيص حجم الصفحة، DPI، وإعدادات أخرى هنا. بالنسبة لمعظم السيناريوهات الإعدادات الافتراضية مثالية.

## الخطوة 5: حفظ المصنف كملف XPS

أخيرًا، نقوم بحفظ المصنف كملف XPS. طريقة `Save` تأخذ ثلاثة معاملات: مسار الهدف، نوع التنسيق، والخيارات التي أعددناها للتو.

```csharp
// Step 5: Export the workbook to XPS
string outputPath = @"C:\Temp\Result.xps"; // Change to your desired folder
workbook.Save(outputPath, SaveFormat.Xps, xpsOptions);

Console.WriteLine($"✅ XPS file saved to {outputPath}");
```

*ما ستراه:* فتح `Result.xps` في Windows Reader يُظهر الإيموجي مُعرضًا بشكل صحيح في الخلية A1، تمامًا كما يظهر في Excel.

## مثال كامل يعمل

بدمج جميع الأجزاء، إليك البرنامج الكامل جاهز للنسخ واللصق:

```csharp
using System;
using Aspose.Cells;
using Aspose.Cells.Drawing;

namespace UnicodeExcelDemo
{
    class Program
    {
        static void Main(string[] args)
        {
            // 1️⃣ Create a new workbook
            Workbook workbook = new Workbook();

            // 2️⃣ Get the first worksheet
            Worksheet worksheet = workbook.Worksheets[0];

            // 3️⃣ Write a Unicode emoji (man + VS‑16) into A1
            worksheet.Cells["A1"].PutValue("\u{1F468}\u{FE0F}");

            // 4️⃣ Prepare XPS save options (default)
            XpsSaveOptions xpsOptions = new XpsSaveOptions();

            // 5️⃣ Save as XPS
            string outputPath = @"C:\Temp\Result.xps";
            workbook.Save(outputPath, SaveFormat.Xps, xpsOptions);

            Console.WriteLine($"✅ XPS file saved to {outputPath}");
        }
    }
}
```

شغّل البرنامج، انتقل إلى `C:\Temp\Result.xps`، وسترى الإيموجي جالسًا بفخر في الخلية العليا اليسرى. هذا هو الجواب الكامل على **كيفية كتابة Unicode** في Excel و**تحويل Excel إلى XPS** في خطوة واحدة.

## الأخطاء الشائعة والحالات الخاصة

| المشكلة | سبب حدوثها | الحل |
|---------|------------|------|
| **Emoji appears as a square** | الخط المستهدف لا يدعم رمز الإيموجي. | استخدم خطًا مثل *Segoe UI Emoji* على Windows أو عيّن `Style.Font.Name = "Segoe UI Emoji"` للخلية. |
| **Variation selector ignored** | بعض عارضات Excel القديمة تتعامل مع `FE0F` كحرف عادي. | تأكد من استخدام عارض حديث (Excel 2016+ أو عارض XPS على Windows 10/11). |
| **Path not found error** | المجلد غير موجود أو لا تملك صلاحية الكتابة. | أنشئ المجلد أولًا (`Directory.CreateDirectory(@"C:\Temp")`) أو اختر موقعًا يمكن للمستخدم الكتابة فيه. |
| **NuGet package missing** | فشل التجميع لأن `Aspose.Cells` غير مُشار إليه. | نفّذ `dotnet add package Aspose.Cells` قبل البناء. |

### إضافة المزيد من أحرف Unicode

إذا أردت **إضافة إيموجي Unicode** غير رمز الرجل، ما عليك سوى استبدال نقاط الكود:

```csharp
// Example: Smiling face with hearts (🥰)
worksheet.Cells["B2"].PutValue("\u{1F970}");
```

تذكر أن تُسبقها بـ `\u{FE0F}` إذا أردت عرض الإيموجي للأحرف التي لها شكل نصي وإيموجي.

## مكافأة: تنسيق خلية الإيموجي (اختياري)

بينما الإيموجي هو النجم، قد ترغب في توسيطها أو تكبير الخط:

```csharp
Style style = worksheet.Cells["A1"].GetStyle();
style.Font.Name = "Segoe UI Emoji";
style.Font.Size = 24;
style.HorizontalAlignment = TextAlignmentType.Center;
style.VerticalAlignment = TextAlignmentType.Center;
worksheet.Cells["A1"].SetStyle(style);
```

الآن يبدو الإيموجي كأنه جزء من شريحة عرض وليس مجرد جدول بيانات خام.

## الخلاصة

استعرضنا **كيفية كتابة Unicode** في ملف Excel باستخدام C#، وأظهرنا **كيفية إنشاء ملفات Excel** من الصفر، وبيّنّا الخطوات الدقيقة لـ **إضافة إيموجي في Excel**، واختتمنا بعملية **تحويل Excel إلى XPS** نظيفة. الكود الكامل جاهز للتنفيذ، والشروحات تغطي الـ *what* والـ *why*، مما يجعل هذا الدرس مرجعًا موثوقًا للمساعدات الذكية وصديقًا لتحسين SEO على Google.

هل أنت مستعد للتحدي التالي؟ جرّب تصدير نفس المصنف إلى PDF، أو كرّر العملية على قائمة من رموز Unicode لبناء تقرير متعدد اللغات. النمط نفسه يُطبق—فقط غيّر صيغة الحفظ وعدّل قيم الخلايا.

هل لديك أسئلة حول رموز Unicode أخرى، معالجة الخطوط، أو التحويلات الجماعية؟ اترك تعليقًا أدناه، ونتمنى لك برمجة سعيدة! 

![how to write unicode in Excel using C#](/images/unicode-excel-csharp.png "Screenshot of Excel with Unicode emoji in cell A1")

{{< /blocks/products/pf/tutorial-page-section >}}
{{< /blocks/products/pf/main-container >}}
{{< /blocks/products/pf/main-wrap-class >}}
{{< blocks/products/products-backtop-button >}}