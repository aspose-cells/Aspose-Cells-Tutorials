---
category: general
date: 2026-02-15
description: تعلم كيفية تضمين الخطوط عند تصدير Excel إلى SVG و XPS، كتابة أحرف Unicode
  بشكل صحيح، وتضمين الخطوط في SVG باستخدام Aspose.Cells.
draft: false
keywords:
- how to embed fonts
- export excel to svg
- how to write unicode
- embed fonts in svg
- how to export xps
language: ar
og_description: كيفية تضمين الخطوط عند تصدير Excel إلى SVG و XPS، كتابة أحرف Unicode،
  وتضمين الخطوط في SVG باستخدام Aspose.Cells.
og_title: كيفية تضمين الخطوط في تصديرات Excel باستخدام C# – خطوة بخطوة
tags:
- Aspose.Cells
- C#
- Excel Export
- Font Embedding
title: كيفية تضمين الخطوط في تصديرات Excel باستخدام C# – دليل كامل
url: /ar/net/working-with-fonts-in-excel/how-to-embed-fonts-in-c-excel-exports-complete-guide/
---

{{< blocks/products/pf/main-wrap-class >}}
{{< blocks/products/pf/main-container >}}
{{< blocks/products/pf/tutorial-page-section >}}

# كيفية تضمين الخطوط في تصدير Excel باستخدام C# – دليل شامل

هل تساءلت يومًا **كيف يتم تضمين الخطوط** في تصدير Excel بحيث يبدو الناتج متطابقًا على كل جهاز؟ لست وحدك. عندما ترسل ورقة عمل إلى عميل لا يمتلك نفس الخطوط المثبتة، قد يصبح المستند مشوشًا، خاصة إذا كان يحتوي على رموز يونيكود خاصة. في هذا الدرس سنستعرض حلًا عمليًا لا يوضح فقط **كيفية تضمين الخطوط**، بل يغطي أيضًا **تصدير Excel إلى SVG**، **كيفية كتابة يونيكود**، و**كيفية تصدير XPS** باستخدام Aspose.Cells.  

بنهاية الدليل ستحصل على مقتطف C# جاهز للتنفيذ يكتب حرف يونيكود مع محدد تنوع، يضم الخطوط المطلوبة، وينتج ملفات XPS و SVG تُظهر بشكل مثالي في أي مكان. لا أدوات خارجية، لا حيل ما بعد المعالجة—كود نظيف ومكتمل ذاتيًا.

## المتطلبات المسبقة

- .NET 6.0 أو أحدث (تعمل الواجهة البرمجية بنفس الطريقة على .NET Framework 4.8)
- Aspose.Cells for .NET (حزمة NuGet `Aspose.Cells`)
- مجلد على القرص حيث يمكن حفظ الملفات المُولدة
- إلمام أساسي بصياغة C# (إذا كنت مبتدئًا تمامًا، فإن الكود مُشروح بشكل مكثف)

إذا كان لديك كل ما سبق، ممتاز—لننتقل مباشرة إلى التنفيذ.

## الخطوة 1: إعداد الـ Workbook والـ Worksheet (كيفية تضمين الخطوط – نقطة الانطلاق)

أول ما نحتاجه هو كائن `Workbook` جديد. فكر في الـ workbook كحاوية لجميع الأوراق، الأنماط، والموارد. إن إنشاؤه سهل، لكنه الأساس لأي عملية **embed fonts in svg** لأن معلومات الخط تعيش على مستوى الـ workbook.

```csharp
using Aspose.Cells;

namespace FontEmbeddingDemo
{
    class Program
    {
        static void Main(string[] args)
        {
            // Step 1: Create a new workbook and grab the first worksheet
            Workbook workbook = new Workbook();               // fresh workbook
            Worksheet ws = workbook.Worksheets[0];            // default sheet
```

> **لماذا هذا مهم:** عندما تقوم لاحقًا بتصدير إلى SVG أو XPS، ينظر Aspose.Cells إلى مجموعة الأنماط في الـ workbook لتحديد أي خطوط يجب تضمينها. بدءًا بـ workbook نظيف يضمن عدم وجود مراجع خطوط عشوائية تلوث الناتج.

## الخطوة 2: كتابة حرف يونيكود مع محدد تنوع (كيفية كتابة يونيكود)

يمكن أن تكون أحرف يونيكود معقدة، خاصة عندما تحتاج إلى شكل حرف محدد. الحرف `𝟘` (MATHEMATICAL DOUBLE‑STRUCK ZERO) مع محدد التنوع‑1 (`\uFE00`) يجبر المُعالج على اختيار العرض “العادي”. هذا مثال مثالي لـ **how to write unicode** لأنه يُظهر السلسلة الدقيقة التي تحتاج لوضعها في خلية.

```csharp
            // Step 2: Write the character '𝟘' followed by Variation Selector-1 into cell A1
            // The literal "\uFE00" is the Variation Selector; it tells the font to use the base glyph.
            ws.Cells["A1"].PutValue("𝟘\uFE00");
```

> **نصيحة:** إذا رأيت صندوقًا فارغًا (�) في الناتج، تحقق من أن الخط المستهدف يدعم الحرف الأساسي *وكذلك* محدد التنوع. ليس كل الخطوط تدعم ذلك.

## الخطوة 3: تصدير الورقة إلى XPS (كيفية تصدير XPS)

XPS هو تنسيق ثابت يشبه PDF لكنه أصلي لنظام Windows. تصدير إلى XPS مع **تضمين الخطوط** يضمن أن المستند سيظهر متطابقًا على أي جهاز Windows، حتى وإن لم يكن الخط مثبتًا محليًا.

```csharp
            // Step 3: Export the worksheet to XPS – fonts are embedded automatically
            string xpsPath = @"C:\Exports\VarSel.xps";
            ws.Cells.ExportToXps(xpsPath);
```

> **ما ستراه:** افتح الملف الناتج `VarSel.xps` في Windows Reader؛ سيظهر الصفر المزدوج بالضبط كما في Excel، مع الحفاظ على النمط الصحيح.

## الخطوة 4: تصدير الورقة إلى SVG مع خطوط مضمَّنة (تضمين الخطوط في SVG)

SVG هو تنسيق صورة متجهة تُعرضه المتصفحات مباشرة. بشكل افتراضي، سيشير Aspose.Cells إلى الخط بالاسم فقط، ما قد يسبب مشاكل حروف مفقودة إذا لم يكن الخط مثبتًا لدى المشاهد. تسمح لك فئة `SvgSaveOptions` **بتضمين الخطوط في SVG**، مما يحول الملف إلى حزمة ذاتية الاكتفاء.

```csharp
            // Step 4: Export to SVG with fonts embedded
            string svgPath = @"C:\Exports\VarSel.svg";
            SvgSaveOptions svgOptions = new SvgSaveOptions
            {
                EmbedFonts = true          // crucial flag – forces font embedding
            };
            ws.Cells.ExportToSvg(svgPath, svgOptions);
```

> **النتيجة:** افتح `VarSel.svg` في أي متصفح حديث (Chrome, Edge, Firefox). سيُعرض حرف يونيكود بشكل صحيح دون الحاجة إلى ملفات خطوط خارجية. إذا فحصت مصدر SVG، ستجد كتلة `<style>` تحتوي على تعريف الخط مشفر بـ Base64.

## مثال كامل يعمل (جميع الخطوات مجمعة)

فيما يلي البرنامج الكامل الذي يمكنك نسخه ولصقه في تطبيق Console. يتضمن جميع الخطوات السابقة، بالإضافة إلى رسالة نهائية في الـ Console لتعرف متى انتهت العملية.

```csharp
using Aspose.Cells;
using System;

namespace FontEmbeddingDemo
{
    class Program
    {
        static void Main(string[] args)
        {
            // Create workbook and worksheet
            Workbook workbook = new Workbook();
            Worksheet ws = workbook.Worksheets[0];

            // Write Unicode character with variation selector
            ws.Cells["A1"].PutValue("𝟘\uFE00");

            // Export to XPS (fonts embedded automatically)
            string xpsPath = @"C:\Exports\VarSel.xps";
            ws.Cells.ExportToXps(xpsPath);
            Console.WriteLine($"XPS exported to: {xpsPath}");

            // Export to SVG with embedded fonts
            string svgPath = @"C:\Exports\VarSel.svg";
            SvgSaveOptions svgOptions = new SvgSaveOptions
            {
                EmbedFonts = true
            };
            ws.Cells.ExportToSvg(svgPath, svgOptions);
            Console.WriteLine($"SVG exported to: {svgPath}");

            Console.WriteLine("All files generated successfully.");
        }
    }
}
```

### المخرجات المتوقعة

- **`VarSel.xps`** – مستند XPS صفحة واحدة يُظهر الصفر المزدوج بالخط المستخدم في Excel.
- **`VarSel.svg`** – ملف SVG يحتوي على تدفق خط مضمّن؛ افتحه في متصفح وسترى نفس الحرف دون أي مربعات حروف مفقودة.

## المشكلات الشائعة & نصائح احترافية (كيفية تضمين الخطوط بفعالية)

| المشكلة | السبب | الحل |
|---------|-------|------|
| الحرف يظهر كمربع في SVG | الخط لم يُضمّن (`EmbedFonts = false`) | عيّن `EmbedFonts = true` في `SvgSaveOptions`. |
| محدد التنوع يتم تجاهله | الخط لا يحتوي على الشكل المتنوع | اختر خطًا يدعم محدد التنوع صراحةً، مثل **Cambria Math** أو **Arial Unicode MS**. |
| فشل التصدير برسالة “Access denied” | المجلد المستهدف للكتابة للقراءة فقط أو غير موجود | تأكد من وجود المجلد (`C:\Exports\`) ومن أن العملية تملك صلاحيات الكتابة. |
| حجم ملف XPS كبير | تضمين خطوط كبيرة غير ضرورية | استخدم خطًا خفيفًا (مثل **Calibri**) إذا كنت تحتاج فقط إلى أحرف لاتينية أساسية. |

> **نصيحة احترافية:** إذا كنت تصدر العديد من الأوراق، أعد استخدام كائن `SvgSaveOptions` واحد لتجنب إنشاء تدفقات خطوط مكررة، ما قد يثقل حجم الـ SVG.

## توسيع الحل (ماذا لو احتجت المزيد؟)

- **تصدير دفعي:** كرر عبر `workbook.Worksheets` واستدعِ `ExportToSvg` لكل ورقة، مع تمرير اسم ملف فريد.
- **استبدال الخطوط المخصص:** استخدم `Style.Font.Name` لإجبار الخط على أن يكون محددًا قبل التصدير. هذا مفيد عندما يستخدم الـ workbook خطًا غير ملائم للترخيص.
- **صور عالية الدقة:** للأنساق القائمة على البكسل (PNG, JPEG) يمكنك ضبط `Resolution` في `ImageOrPrintOptions` – ليس ضروريًا للـ SVG، لكنه مفيد إذا قررت لاحقًا إنشاء معاينات PNG.

## الخلاصة

غطّينا **كيفية تضمين الخطوط** في كل من تصدير XPS و SVG، وأظهرنا **كيفية كتابة يونيكود** مع محددات التنوع، وبيّنّا لك **كيفية تصدير Excel إلى SVG** مع الحفاظ على الخطوط داخل الملف. باتباع الخطوات أعلاه، تتخلص من مشكلة “الخط مفقود” وتضمن أن أي شخص—بغض النظر عن الخطوط المثبتة على جهازه—يرى ما قصدت عرضه.

هل أنت مستعد للتحدي التالي؟ جرّب تضمين خط TrueType مخصص غير مثبت على الخادم، أو جرب تصدير إلى PDF مع الحفاظ على الخطوط المضمَّنة. كلا المسارين يبنيان على نفس المبادئ التي استعرضناها هنا.

برمجة سعيدة، ولتظل مستنداتك المُصدَّرة دائمًا ذات جودة بكسل‑مثالية!

{{< /blocks/products/pf/tutorial-page-section >}}
{{< /blocks/products/pf/main-container >}}
{{< /blocks/products/pf/main-wrap-class >}}
{{< blocks/products/products-backtop-button >}}