---
category: general
date: 2026-02-14
description: تعلم كيفية حفظ ملف Excel كنص باستخدام C#. يغطي هذا الدليل خطوة بخطوة
  تصدير Excel إلى txt، تحويل جدول البيانات إلى txt، والتعامل مع المشكلات الشائعة.
draft: false
keywords:
- save excel as text
- export excel to txt
- convert spreadsheet to txt
- how to save txt
- convert xlsx to txt
language: ar
og_description: احفظ ملف Excel كنص في C# مع مثال كامل للكود. صدّر Excel إلى txt، حوّل
  جدول البيانات إلى txt وتجنّب الأخطاء الشائعة.
og_title: حفظ إكسل كنص – دليل C# الكامل
tags:
- C#
- Aspose.Cells
- Excel automation
title: حفظ إكسل كنص – دليل C# الكامل لتصدير إكسل إلى ملف TXT
url: /ar/net/converting-excel-files-to-other-formats/save-excel-as-text-complete-c-guide-to-export-excel-to-txt/
---

{{< blocks/products/pf/main-wrap-class >}}
{{< blocks/products/pf/main-container >}}
{{< blocks/products/pf/tutorial-page-section >}}

# حفظ Excel كنص – دليل C# كامل

هل احتجت يومًا إلى **حفظ Excel كنص** لكن لم تكن متأكدًا من أي استدعاء API تستخدم؟ أنت لست وحدك. يواجه العديد من المطورين صعوبة عندما يحاولون **تصدير Excel إلى txt** لأن مكتبات التفاعل الافتراضية غير مرنة وبطيئة.  

في هذا الدرس سنستعرض حلًا نظيفًا وجاهزًا للإنتاج يحول مصنف *.xlsx* إلى ملف نصي *.txt*، كل ذلك ببضع أسطر من C#. في النهاية ستعرف كيف **تحول جدول البيانات إلى txt**، وتضبط خيارات التقريب، وتتجنب الأخطاء الشائعة عند **تحويل xlsx إلى txt**.

> **ما ستحصل عليه:** برنامج كامل قابل للتنفيذ، شرح لماذا كل سطر مهم، ونصائح لتوسيع المنطق إلى مصنفات أكبر أو محددات مخصصة.

---

## المتطلبات المسبقة

قبل أن نبدأ، تأكد من وجود ما يلي:

* .NET 6.0 أو أحدث (الكود يعمل على .NET Core و .NET Framework على حد سواء).  
* حزمة **Aspose.Cells for .NET** على NuGet – فهي توفر الفئات `Workbook` و `TxtSaveOptions` التي سنستخدمها.  
* ملف Excel بسيط (`nums.xlsx`) موجود في مكان يمكنك الإشارة إليه بمسار مطلق أو نسبي.  

إذا لم تقم بتثبيت Aspose.Cells بعد، نفّذ:

```bash
dotnet add package Aspose.Cells
```

هذا كل شيء—بدون COM interop، ولا حاجة لتثبيت Office.

---

## الخطوة 1: تحميل مصنف Excel

أول شيء نحتاجه هو نسخة من `Workbook` تشير إلى ملف المصدر. فكر في `Workbook` كتمثيل في الذاكرة للمستند Excel بالكامل.

```csharp
using Aspose.Cells;
using System;

class Program
{
    static void Main()
    {
        // 🔹 Load the Excel workbook from disk
        Workbook workbook = new Workbook("YOUR_DIRECTORY/nums.xlsx");
```

**لماذا هذا مهم:**  
`Workbook` يقرأ الملف مرة واحدة، يبني كائنات الخلايا، ويحافظ على معلومات النمط جاهزة لأي عملية تصدير لاحقة. تحميله مبكرًا يتيح لك فحص عدد الأوراق أو التحقق من البيانات قبل كتابة ملف النص.

---

## الخطوة 2: ضبط خيارات حفظ النص (تصدير Excel إلى TXT)

توفر لنا Aspose.Cells الفئة `TxtSaveOptions` التي يمكن من خلالها ضبط كيفية عرض الأرقام. في هذا المثال نحدّ الدقة إلى **أربعة أرقام معنوية** ونقربها، مما يجعل ملف النص مرتبًا.

```csharp
        // 🔹 Set up how the data will be written to .txt
        TxtSaveOptions saveOptions = new TxtSaveOptions
        {
            // Keep numbers readable – 4 significant digits, rounded
            SignificantDigits = 4,
            DigitsMode = DigitsMode.Round
        };
```

**لماذا قد ترغب في تغيير ذلك:**  
إذا كان جدول البيانات يحتوي على بيانات علمية، قد تحتاج إلى مزيد من الأرقام أو وضع تقريب مختلف. `TxtSaveOptions` يدعم أيضًا محددات مخصصة (علامة تبويب، فاصلة، فاصلة منقوطة) وترميز—مثالي للمشاريع الدولية.

---

## الخطوة 3: حفظ المصنف كملف نصي (تحويل جدول البيانات إلى TXT)

الآن يحدث العمل الشاق. نمرر `Workbook` و `TxtSaveOptions` المُعدّين إلى `Save`، الذي يكتب تمثيلًا نصيًا للورقة النشطة.

```csharp
        // 🔹 Export the workbook to a .txt file using the options above
        workbook.Save("YOUR_DIRECTORY/nums.txt", saveOptions);

        Console.WriteLine("✅ Excel file has been saved as text!");
    }
}
```

**ما ستراه:** ملف `.txt` مفصول بعلامات تبويب حيث تحترم قيمة كل خلية قاعدة التقريب إلى أربعة أرقام. افتحه في Notepad أو أي محرر، وسترى شيئًا مثل:

```
12.34	56.78	90.12
3.1416	2.718	1.618
```

إذا فتحت الملف مرة أخرى في Excel (البيانات → من نص)، ستصطف الأرقام تمامًا كما ظهرت في المصنف الأصلي.

---

## تصدير Excel إلى TXT – اختيار المحدد

بشكل افتراضي يستخدم Aspose محدد **علامة تبويب** (`\t`)، وهو مثالي لمعظم سيناريوهات تحويل الجداول إلى نص. ومع ذلك، قد تحتاج إلى **فاصلة** لعمليات تدفق CSV المتوافقة.

```csharp
        TxtSaveOptions csvOptions = new TxtSaveOptions
        {
            Delimiter = ',',
            SignificantDigits = 6,
            DigitsMode = DigitsMode.Round
        };
        workbook.Save("YOUR_DIRECTORY/nums_comma.txt", csvOptions);
```

**نصيحة:** عندما تخطط لإدخال الملف في نظام آخر (مثل أداة تحميل دفعي لقاعدة بيانات)، تحقق مرة أخرى من المحدد المطلوب والترميز (`Encoding` property) لتجنب فساد البيانات.

---

## تحويل Xlsx إلى Txt – التعامل مع أوراق عمل متعددة

المثال أعلاه يصدر فقط **الورقة النشطة**. إذا كان المصنف يحتوي على عدة علامات وتحتاج كل واحدة كملف نصي منفصل، يمكنك التكرار عبر مجموعة `Worksheets`:

```csharp
        foreach (Worksheet sheet in workbook.Worksheets)
        {
            // Activate the sheet before saving
            workbook.Worksheets.ActiveSheetIndex = sheet.Index;

            string txtPath = $"YOUR_DIRECTORY/{sheet.Name}.txt";
            workbook.Save(txtPath, saveOptions);
            Console.WriteLine($"📄 Saved sheet '{sheet.Name}' to {txtPath}");
        }
```

**لماذا هذا مفيد:**  
غالبًا ما تولد خطوط أنابيب التقارير الكبيرة ورقة واحدة لكل عميل أو لكل شهر. أتمتة الفصل توفر ساعات من النسخ اليدوي.

---

## المشكلات الشائعة عند تحويل Xlsx إلى Txt

| المشكلة | ما يحدث | كيفية الإصلاح |
|---------|----------|----------------|
| **غياب ترخيص Aspose.Cells** | المكتبة تُظهر علامة مائية تجريبية أو تقيد عدد الصفوف. | اشترِ ترخيصًا أو استخدم وضع التقييم المجاني للملفات الصغيرة. |
| **ترميز خاطئ** | الأحرف غير ASCII تصبح مشوهة (مثل الأحرف المت accent). | عيّن `saveOptions.Encoding = Encoding.UTF8;` |
| **أوراق عمل كبيرة (>1 مليون صف)** | استهلاك الذاكرة يرتفع، قد يتعطل البرنامج. | استخدم `Workbook.LoadOptions` مع `MemorySetting` مضبوطًا على `MemorySetting.MemoryPreference` أو عالج الورقة على دفعات. |
| **محدد غير متوقع في البيانات** | علامات التبويب داخل قيم الخلايا تُفسد محاذاة الأعمدة. | بدّل إلى محدد أقل شيوعًا (مثل `|`) واستبدل علامات التبويب في البيانات مسبقًا. |

معالجة هذه القضايا مسبقًا تجعل حل **كيفية حفظ txt** قويًا للبيئات الإنتاجية.

---

## نصيحة احترافية: التحقق من المخرجات برمجيًا

بدلاً من فتح الملف يدويًا، يمكنك قراءة الأسطر القليلة الأولى مرة أخرى إلى C# لتأكيد نجاح التصدير:

```csharp
using System.IO;

string[] lines = File.ReadAllLines("YOUR_DIRECTORY/nums.txt");
Console.WriteLine("First line of exported text:");
Console.WriteLine(lines.Length > 0 ? lines[0] : "File is empty!");
```

هذا الفحص السريع مفيد في خطوط CI حيث تريد التأكد من أن التحويل لم ينتج ملفًا فارغًا.

---

## توضيح صورة

![مثال حفظ Excel كنص](image-placeholder.png){:alt="مثال حفظ Excel كنص"}

الصورة أعلاه تُظهر عرضًا نموذجيًا في Notepad للملف `.txt` المُولد، مؤكدة أن الأرقام مُقربة إلى أربعة أرقام معنوية.

---

## ملخص وخطوات قادمة

غطّينا كامل سير عمل **حفظ Excel كنص**:

1. حمّل المصنف باستخدام `Workbook`.  
2. اضبط `TxtSaveOptions` (الأرقام المعنوية، التقريب، المحدد).  
3. استدعِ `Save` لإنتاج ملف نصي.

الآن تعرف كيف **تصدّر Excel إلى txt**، **تحوّل جدول البيانات إلى txt**، وتتعامل مع تفاصيل **تحويل xlsx إلى txt** للمصنفات متعددة الأوراق.  

**ما التالي؟**  

* جرّب التصدير إلى CSV (`CsvSaveOptions`) لاستيراد متوافق مع Excel.  
* استكشف `HtmlSaveOptions` إذا احتجت معاينة سريعة للورقة بصيغة HTML.  
* دمج هذا الكود مع خدمة مراقبة ملفات لتحويل ملفات Excel الواردة تلقائيًا في مجلد.

لا تتردد في التجربة—غيّر المحدد، عدّل دقة الأرقام، أو حتى بثّ المخرجات مباشرة إلى مقبس شبكة. الـ API مرن، وبمجرد إتقان الأساسيات يصبح توسيعه سهلًا كقطعة من الكعك.

*برمجة سعيدة! إذا واجهت أي صعوبات، اترك تعليقًا أدناه أو تواصل مع منتديات مجتمع Aspose. نحن جميعًا هنا لمساعدتك.*

{{< /blocks/products/pf/tutorial-page-section >}}
{{< /blocks/products/pf/main-container >}}
{{< /blocks/products/pf/main-wrap-class >}}
{{< blocks/products/products-backtop-button >}}